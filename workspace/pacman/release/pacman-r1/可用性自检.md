# 可用性自检记录：pacman v1.0.0（release r1）

> 自检时间：2026-08-11（UTC）｜环境：Linux 5.15.0-186-generic / CPython 3.11（本机，与开发/测试环境一致）
> 自检范围：产品包解包后可安装、可运行、测试全绿；真终端 PTY 冒烟通过。

## 1. 产品包完整性

- `pacman-v1.0.0.tar.gz` 解包成功（tar -xzf），包含 15 个文件：`pacman/` 包（9 源模块 + data/map_classic.txt）+ README.md + requirements.txt + run.py；
- 已排除 `__pycache__`/`*.pyc`（打包时 --exclude）；
- `sha256sum -c SHA256SUMS` 校验一致（`51693531abe5b338...`）。

## 2. 编译检查

```
$ python3 -m compileall -q pacman/   （解包目录）
→ 0 退出，无语法错误
```

## 3. 启动冒烟（CLI）

```
$ python3 -m pacman --help
→ 正常输出 usage 与全部 7 个参数说明（--map/--ghosts/--lives/--level/--speed/--no-color/--log-ai）
```

## 4. 地图加载（内置 22×19）

```
$ python3 -c "from pacman.map import load_map; m=load_map('pacman/data/map_classic.txt'); print(m.initial_dots)"
→ 216
```

## 5. 完整回归测试（对解包后的产品包代码运行）

```
$ PACMAN_CODE_DIR=<解包目录> python3 -m unittest discover -s tests -t .
→ Ran 194 tests in 0.18s — OK（194/194 全绿）
```
> 说明：测试套件来自 `tests/pacman-r1/`（test 阶段终版），以解包后的产品包为被测代码，非直接引用开发目录。

## 6. 系统层 PTY 冒烟（真终端进程）

```
$ python3 scripts/e2e_pty.py --map <绝对路径>/pacman/data/map_classic.txt
→ run=1 rc=0 startup=0.071s rendered=True bytes=580
```
> 通过：真实 PTY 中启动成功（0.07s ≪ 3s 验收线）、完整渲染检测通过（含中文 HUD「分数:」）、q 退出码 0。
> 注：脚本默认地图路径问题（D-01，已登记为发布说明已知限制 7）需显式 `--map` 绝对路径；逻辑层测试不受影响。

## 7. 零网络验证（NFR-06）

```
$ python3 scripts/network_strace.py
→ rc=0 network_calls=0
```

## 8. FR-10 主验收复验（四幽灵目标互异）

- 权威证据：测试套件 U-20 `TestTargetCellFourDiffer`（194 全绿中包含）——同一局面四幽灵 target 互异；
- releaser 另用等价脚本复验：targets = {BLINKY:(5,5), PINKY:(5,9), INKY:(7,11), CLYDE:(18,1)}，4 个互异 → PASS。
- 注：README §9 自检命令 4 存在索引笔误（`g[3]` 越界），不影响实际功能；已登记发布说明已知限制 8。

## 9. 环境限制说明

- 本自检在开发机真实终端完成；未在「干净裸机/CI 沙箱」重复执行（无独立干净环境）。依据：
  - 零第三方依赖（requirements.txt 为空声明）已由 test_arch.py 静态验证；
  - 干净环境可跑（NFR-03）以 README 双路径声明 + 本机实测为依据；
  - 跨平台（macOS/Windows）不在验收范围（需求限定 Linux）。
- 性能用例（FPS ≥10 / 响应 ≤100ms / 5 分钟稳定）未做长时自动化采样，tick=100ms + time.monotonic 设计保证 + PTY 启动耗时实测 0.07s 佐证；已登记发布说明已知限制 9。

## 10. 结论

**通过**：产品包解包即用、编译零错误、启动正常、194/194 测试全绿、PTY 真终端冒烟通过、零网络行为、FR-10 主验收复验通过。环境限制与遗留项均已在发布说明 §5 如实登记，不阻塞发布。
