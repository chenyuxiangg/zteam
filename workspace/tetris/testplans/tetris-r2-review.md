# 测试方案评审：tetris（r2）

## 0. 评审结论
- 结论：**PASS**
- 一句话理由：10 项检查全部通过，需求全覆盖、方案对齐、边界异常充分、可追溯可执行
- 评审轮次：r2

## 1. 检查清单核对表

| # | 检查项 | 结论（PASS/FAIL） | 依据/缺失说明 |
|---|--------|------------------|-------------|
| 1 | 需求全覆盖 | PASS | §3.1 映射表覆盖全部 FR-01~FR-26（FR-17 作废有明确理由，见 §3.3）与 NFR-01~NFR-06，无遗漏。Q-05（Python 版本矩阵）由 TC-S-06 覆盖。对照分析 r2 逐一核对：26 个 FR（1 作废）、6 个 NFR 均有对应用例 |
| 2 | 方案对齐 | PASS | 用例挂载点与方案 §2~§5 一致：坐标系统（x 右 y 下，10×20）、数据结构（Point/board[y][x]/TETROMINOES）、简化旋转（无 wall kick，Q-02 默认）、--tick/--no-color 参数（§4.1）、MIN_COLS=42/MIN_LINES=26 推导（§3.1）、模型层纯逻辑可脱离终端测试（§2）。TC-U-01 形状断言基准直接引用方案 §4.2 占格坐标表，方案 r2 增量 a 已落地 |
| 3 | 可执行性 | PASS | 每用例步骤/预期结果具体可照做。单元用例直接构造 GameState 注入场景（手工填 board/piece_type/rotation），预期含具体断言值（坐标/得分/状态/计时）；集成用例指定按键序列（WASD/方向键/空格/P/q）与屏幕内容 expect；性能用例明确测量方法（time.monotonic + pyte ≤10ms 轮询 + 采样 ≥20 次 + P95） |
| 4 | 边界与异常 | PASS | 边界覆盖充分：非法输入（tick 越界 TC-U-16）、环境异常（非 TTY TC-I-02、终端过小 TC-I-03、运行中 resize TC-I-13）、信号中断（Ctrl+C 三时机 TC-I-08、SIGTERM TC-I-09、q TC-I-07）、逻辑边界（旋转碰撞拒绝 TC-U-06、贴壁移动拒绝 TC-U-05、多行同消 TC-U-09、差一格不满 TC-U-18、撞顶 TC-U-12、负 y 不越界 TC-U-19、O 旋转不变 TC-U-02、计分累计 TC-U-20）、时间类边界（tick 偏差/锁定生成/HUD 刷新/按键响应/硬降时限/暂停不吞时间 6 处绝对值化）。超时/重试/并发/资源耗尽已声明不适用并给出理由（本地单进程、无网络 I/O、curses timeout(25) 天然覆盖） |
| 5 | 可追溯性 | PASS | §3.1 需求映射表列出每需求对应的用例数与关键编号，每用例标注关联需求编号，双向映射清晰。验证路径：FR-05 → TC-U-01/02 + TC-I-05，FR-09 → TC-U-02/06 + TC-I-05，NFR-03 → TC-I-08，均可反查 |
| 6 | 优先级合理 | PASS | P0 覆盖核心路径与安全：方块生命周期闭环（生成/下落/旋转/硬降/锁定/消行/计分/撞顶 TC-U-01~12）与终端退出（q/Ctrl+C/termios 恢复 TC-I-01/05~08/16、TC-S-01）。P1 覆盖主要功能：软降（TC-U-13）、next 预览（TC-U-14）、暂停（TC-U-15）、tick 配置（TC-U-16）、边界（TC-U-18~20）、HUD（TC-I-10）、结束画面（TC-I-11）、SIGTERM（TC-I-09）、README（TC-S-02）、错误提示（TC-S-04）、性能建议项（TC-P-01/02）。P2 为非阻塞项：颜色开关（TC-I-15、TC-U-17）、resize 恢复（TC-I-13）、终端矩阵（TC-S-03）、代码走查（TC-S-05）、Python 版本（TC-S-06）。分级合理 |
| 7 | 环境与数据 | PASS | §4 明确：主环境（Ubuntu 22.04/24.04 干净容器）、Python 矩阵（3.6/3.8/3.11/3.12）、终端矩阵（GNOME Terminal/Konsole/xterm/SSH）含优先级缩减策略；测试侧依赖（pytest/pexpect/pyte）声明不影响交付物零依赖；数据准备全部通过编程构造（注入 board/piece_type 等字段），不依赖随机序列，确定性可复现；限制已声明（无 TTY CI 仅跑单元层、性能指标为建议项、终端过小退出码不绑定具体值） |
| 8 | 自动化合理性 | PASS | §5 分层选型有据：单元层 pytest（模型纯逻辑无 curses 依赖，天然自动化）；集成层 pexpect + pyte（真实 PTY 驱动，比 mock curses 更接近真实终端行为，对齐 snake-linux 已验证模式）；系统层 bash 脚本 + 人工清单；性能 bash + pidstat + pyte 细粒度轮询；明确不选 pytest-cov（第一版以需求覆盖为准）与 mock 框架（逻辑层无外部依赖可 mock） |
| 9 | 不越界 | PASS | §1 范围外明确声明「不写完整测试代码（本方案提供用例步骤与预期，测试代码由 test-developer 落地）」。全文为测试方案设计，未含实际测试代码文件，未修改开发方案 |
| 10 | 可审计 | PASS | 新文件 tetris-r2.md 不覆盖 r1。§9 修改回应表齐全（14 条）：回应 r1 评审建议 1 + 遗留 3 条、分析 R1-01~R1-05、方案 r2 增量 a/b/c、事实对齐 code/test r1 已通过。文末附本轮变更清单（8 项增量） |

## 2. 评审意见列表

- **[建议]** TC-U-15 断言已按 r1 评审建议收紧为确定断言（PAUSED 期间 step/move/rotate/soft_drop 全部无副作用），与方案 code r1 实际实现一致，无改进空间。
  - 依据：测试方案 §3.2 TC-U-15 预期列 + §9 修改回应表「r1 评审建议 1」条目

- **[建议]** TC-P-01 测量方法已按 r1 遗留 3 明确化（time.monotonic + pyte 细粒度 ≤10ms 轮询 + 开销分离记录），符合 r2 绝对值口径要求。注意：本轮测试方案定位为设计文档，实际测量值以 test-developer 落地结果为准；若细粒度轮询后仍超 50ms P95，不阻塞发布（NFR-01 为建议项，analysis r2 R1-04），记录实测数据即可。
  - 依据：测试方案 §3.2 TC-P-01、§4 数据准备、§5 执行方式

- **[建议]** 软降集成测试 TC-I-05 的分工已按 r1 遗留 2 明确为「粗粒度检查（快速连发 S/↓ 期间方块高度变化明显快于自动下落）」，精确逐格行为由单元层 TC-U-13 覆盖。此分工合理，PE 断言稳定性可接受。test r1 已按此分工通过，无需调整。
  - 依据：测试方案 §2 策略、§3.2 TC-I-05、§6 风险表、§8 待确认

## 3. 遗留事项（仅 PASS 时）

- 无阻塞遗留项。后续 test-developer 落地时关注：
  1. TC-P-01 细粒度轮询（≤10ms）的实际开销与测量值，结果记录于测试报告中；
  2. 终端矩阵实机若不足 4 种（Q-07），按 GNOME Terminal ≥ xterm ≥ SSH ≥ Konsole 优先级缩减并留痕；
  3. 若需求方后续拍板变更 Q-01~Q-04 任一默认值（计分/旋转/随机/等级曲线），仅需调整对应用例断言数值或增删局部用例，存量结构不受影响；
  4. NFR-01/02 为建议项，若需求方确认不关注可取消 TC-P-01/02（标注已到位）。
