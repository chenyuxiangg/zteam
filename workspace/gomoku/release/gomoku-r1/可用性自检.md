# 可用性自检记录：gomoku v0.4.0（r1）

**执行时间**：2026-08-11 10:11~10:13 UTC
**执行环境**：本机（zteam 工作区所在 Linux 主机），Python 3.11（/home/zyzs/.hermes/hermes-agent/venv），x86-64
**结论**：**产品可用性 PASS**——安装/导入/CLI/自检/端到端冒烟全部通过；144 例 pytest 全绿（质量门禁复现），本次全量跑出现 1 例已知测试脆弱性（详见 §5）。

## 1. 环境准备
- 依赖检查：`rich` 已安装（产品唯一运行时依赖）；`pytest`/`pexpect` 可用（dev）；
- 测试基线：`workspace/gomoku/tests/gomoku-r1`（pytest.ini 自动注入 code 路径，无需 pip install）；
- 打包基线：`workspace/gomoku/code/gomoku-r1`（code 阶段终版，评审 PASS）。

## 2. 打包产物核验
- `gomoku-v0.4.0.tar.gz` 生成成功，内容 14 项：gomoku 包 9 个 .py + py.typed、README.md、pyproject.toml、scripts/smoke.sh（已排除 __pycache__/egg-info/.gitignore 构建杂物）；
- `SHA256SUMS` 已生成并与包一致：`sha256sum -c SHA256SUMS` → OK（728a8bcddd0fc1d9bf30ec03e9dc17acd824fa4dc3fb7b023dc7d597bf4c85c0）。

## 3. 实际运行测试结果

### 3.1 单元/集成测试（pytest 全量）
```bash
cd tests/gomoku-r1 && bash scripts/run_tests.sh
```
- **本次运行：143 passed, 1 failed, 32.60s**（详见 §5 脆弱性说明；失败用例为 test_integration 已知随机脆弱用例）
- **quality 门禁同基线运行：144 passed, 0 failures, 32.66s**（workspace/gomoku/quality/gomoku-r1.md §2 实录）
- **test 评审同基线运行：145 passed, 0 failures, 32.72s**（gomoku-r1-review.md §2 实录）
- 通过项覆盖：test_board（胜负 8 向+边界、坐标解析、越界）、test_forbidden（禁手对照表 27+7 例，含红线 A1~A4）、test_ai（封堵/活三/弱档合法性/禁手规避/耗时 P95）、test_config、test_ui、test_integration（回合/重开/退出/CLI）。

### 3.2 产品自检（code 目录实跑）
```bash
PYTHONPATH=. python3 -c "import gomoku; ..."      # import OK, version 0.4.0 ✓
PYTHONPATH=. python3 -m gomoku.forbidden_cases     # 24/24 PASSED ✓
PYTHONPATH=. python3 -m gomoku.ai_self_check       # 4/4 PASSED ✓
```
AI 自检明细：open-four 封堵 10/10、活三响应 10/10、弱档合法 10/10、禁手预过滤 1/1、**strong 中盘 20 子实测 1503ms ≤ 2s（NFR-01 达标）**。

### 3.3 CLI 冒烟
- `gomoku --version` → `gomoku 0.4.0` ✓
- `gomoku --help` → 全部参数正常输出（--size/--difficulty/--forbidden/--human/--version/--debug-timing）✓

### 3.4 端到端冒烟（smoke.sh，5 步全过）
```bash
bash scripts/smoke.sh
```
1. import OK（v0.4.0）✓
2. 禁手自检 24/24 ✓
3. AI 自检 4/4（strong 中盘 1508ms）✓
4. CLI --help/--version ✓
5. **端到端整局：pipe 已知胜序列（H8→I9→J10→K11→L12），weak AI 下黑方胜局横幅 "Black wins" 正确出现** ✓
6. 非法输入 fuzz（越界/乱码/空格格式）：全部按预期提示错误、不崩溃 ✓

## 4. 安装路径验证（打包可分发性）
- `pip install .` 安装方式已验证（quality 门禁 §1-4：pip install -e . 可安装可执行；console script `gomoku` 生效）；
- 本自检以 `PYTHONPATH=. python3 -m gomoku` 与包内 smoke.sh 双路径执行，均通过；未在隔离 venv 重复 pip 安装（与 quality 门禁 §2 实录一致，不重复占用时间）。

## 5. 发现的问题与说明
| # | 问题 | 性质 | 证据 | 处置 |
|---|------|------|------|------|
| 1 | 全量 pytest 偶发 1 例失败：`test_human_wins_with_known_sequence`（test_integration） | **测试脆弱性，非产品缺陷** | 该用例断言"人类按固定序列执黑 10 步内获胜"，隐含假设 weak AI 不封堵人类冲四线；weak 档为邻域合法随机落子，本次运行 AI 恰好挡了 E1/G1 两点破坏序列 | 单独复跑该用例 3 次均通过（0.17s each）；smoke.sh 端到端胜局正常；quality 门禁 144/144、test 评审 145/145 均全绿。已在发布说明 §5 登记，建议后续固定 weak 随机种子或改构造性断言 |
| 2 | strong AI 中盘实测 1503~1508ms | 符合预期（NFR-01 ≤2s 达标） | 两次独立实测均 <2s | 记录，无处置 |

## 6. 自检结论
- 产品可安装、可导入、可启动、可完整对局、胜负判定正确、非法输入健壮、退出干净；
- 全部质量红线核查项（运行/安装/冒烟）通过；唯一异常为测试套件自身的随机脆弱用例（非产品行为缺陷），已如实登记；
- **判定：本发布包可用性 PASS，可交付。**
