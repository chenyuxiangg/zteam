# 问题单 MTO-4-01

状态：closed
严重级：P0
提单人：MTO
时间：2026-08-15T06:38:29Z

描述：真实渲染路径 Renderer.render() 首帧即崩：TypeError: invalid color argument @ gui_renderer/renderer.py:337 screen.fill(self._skin.background)。根因：gui_renderer.types.Color 是自定义 frozen dataclass（r/g/b 字段），pygame Surface.fill()/draw 系列 API 仅接受 tuple/str/pygame.Color，不接受自定义 dataclass。真实集成（SDL dummy + 真实 pygame 2.6.1）下 game-app _render() 调用链 app.py:724 → renderer.render() 首帧必炸；scripts/bench_fps.py / bench_memory.py 的 run_benchmark 同因崩溃（NFR-01/02 无法实测）。gui-renderer iter-3 IT 全部走 fake 桩（fake_pg.draw.calls）故未暴露，本模块 IT 真实集成首次暴露。修复建议（gui-renderer 侧最小侵入）：renderer.py 内所有传给 pygame API 的颜色参数先转 tuple (c.r, c.g, c.b) 或 pygame.Color(c.r, c.g, c.b)，涉及 background/grid_line/snake_head/snake_body/food/food_outline/hud_text/hud_accent 及插值绘制处。影响：FR-06/07 渲染闭环、FR-10 皮肤、NFR-01/02 性能脚本、NFR-03。

## 修复记录

- 2026-08-15 FO 修复：
  1. **根因**：gui_renderer.types.Color 是自定义 frozen dataclass，pygame Surface.fill()/draw 系列/font.render 仅接受 tuple/str/pygame.Color。
  2. **修改（gui-renderer iter-3，最小侵入）**：`code/gui-renderer/iter-3/gui_renderer/renderer.py` 新增 `_pygame_color()` 工具函数（Color dataclass → tuple (r,g,b)，已传 tuple/str/pygame.Color 幂等原样返回），并应用到全部传给 pygame API 的颜色参数：背景填充 screen.fill、蛇头/蛇身 draw.rect、食物三形态（solid/ringed/checkered）与 outline/镂空、HUD font.render（hud_text/hud_accent/status_color）。
  3. **连带缺陷（同问题单影响面 NFR-02）**：`snake-linux/scripts/bench_memory.py` run_benchmark 用 HARD 难度且无方向切换，2 秒内蛇必撞墙 OVER → `_tick` 入口断言崩溃（此前被颜色崩溃掩盖，颜色修复后暴露）。已对齐 bench_fps：按 BENCH_AI_DIRECTION_SWITCH_INTERVAL_S 周期切换方向。
  4. **断言同步**：gui-renderer UT（4 处）+ IT（21 处）中"传给 pygame 的颜色 == Skin.Color"断言更新为 tuple 值比较（行为变更：pygame 收到 tuple）。
  5. **验证**：gui-renderer UT 99 passed / IT 53 passed；game-app UT 186 passed / IT 28 passed（含真实渲染闭环 test_04_02、bench_fps/bench_memory 真实执行）；三皮肤×三食物形态×插值×resize 真实渲染冒烟通过。

- 2026-08-15 FO 二次修复（MTO 复测驳回：连带缺陷修复不彻底，NFR-02 仍无法实测）：
  1. **根因**：bench_memory.py（及同序列的 bench_fps.py）方向切换序列 `["up","down","left","right"]` 含反向项——`down` 是 `up` 的反向、`right` 是 `left` 的反向；game_core `set_direction` 对反向输入（蛇长≥2）静默忽略（架构设计，game-core UT #9a 固化）→ 蛇实际只沿 up/left 直行 → HARD 下撞墙 OVER → `_tick` 入口断言（app.py:618-620）崩溃。实测复现：`--duration 3` 必崩（AssertionError，与 MTO 复测一致）。
  2. **修改（最小侵入）**：两脚本方向序列改为无反向项循环 `["up","left","down","right"]`（相邻项均正交，永不触发反向忽略；蛇走矩形回路不撞墙），抽为模块级常量 `BENCH_DIRECTION_CYCLE`，切换处改用 `BENCH_DIRECTION_CYCLE[direction_cycle % len(...)]`，并注明根因注释。涉及 `snake-linux/scripts/bench_memory.py`（NFR-02）与 `snake-linux/scripts/bench_fps.py`（NFR-01，同序列同病，一并修复）。
  3. **防回归 UT**：`tests/test_game_app/test_app_iter4_bench.py` 两测试类各新增 `test_direction_cycle_no_reversal`，断言序列相邻项（含首尾）互不为反向——旧序列必失败，防再次回退。
  4. **验证**：game-app UT 188 passed（+2 新防回归）；IT 28 passed；真实执行 bench_memory `--duration 3`（MTO 复测标准）与 `--duration 10` 均 PASS（峰值 144MB）；bench_fps `--duration 3` / `--duration 10` 均 PASS（P95≈2-3ms）。

## 复测记录
- 2026-08-15T10:07:28Z FO 修复完成
- 2026-08-15T12:10:00Z MTO 复测：**不通过，改回 open**。主缺陷（Color dataclass → pygame 颜色 TypeError）确认已修复：gui-renderer UT 99 passed / IT 53 passed；game-app UT 186 passed / IT 28 passed；真实集成冒烟（3 皮肤 × 20 帧真实渲染闭环）通过。但**连带缺陷修复不彻底（影响面 NFR-02）**：`scripts/bench_memory.py` 的方向切换序列 `["up","down","left","right"]` 含反向项——`down` 是 `up` 的反向、`right` 是 `left` 的反向，而 game_core `set_direction` 对反向（蛇长≥2）静默忽略 → 蛇实际直行，HARD 难度下撞墙 OVER → `_tick` 入口断言崩溃。**实测：`--duration 2`（IT 用例参数）侥幸通过；`--duration 3` 必崩（AssertionError @ app.py:618，复跑 2/2 复现）；脚本默认 `BENCH_DURATION_SECONDS=60` 默认运行必崩 → NFR-02 仍无法实测**。IT 用例 test_04_15 用 2s 恰好绕过崩溃窗口，未暴露。修复方向：方向切换序列改用无反向项循环（如 up→left→down→right）或每次切换后跳过反向项。
- 2026-08-15T12:12:09Z FO 修复完成
- 2026-08-15T14:30:00Z MTO 复测：**通过，close**。
  1. **主缺陷（Color dataclass → pygame TypeError）已修复**：gui-renderer iter-3 `renderer.py` 新增 `_pygame_color()`（dataclass→tuple，tuple/str/pygame.Color 幂等），覆盖 screen.fill / draw.rect（蛇头/蛇身/食物三形态/outline/镂空）/ font.render（hud_text/hud_accent/status_color）全部 pygame API 颜色参数。实测：gui-renderer UT 99 passed / IT 53 passed；game-app UT 188 passed（含 +2 防回归）/ IT 28 passed；真实集成冒烟——三皮肤（classic/dark/colorblind_friendly）× 每皮肤 20 帧真实渲染闭环（SDL dummy + 真实 pygame 2.6.1）无异常。
  2. **连带缺陷（NFR-01/02 bench 方向序列含反向项→撞墙断言崩溃）已彻底修复**：bench_memory.py / bench_fps.py 方向序列改为无反向项循环 `BENCH_DIRECTION_CYCLE = ["up","left","down","right"]`（相邻项正交，蛇走矩形回路不撞墙），切换处 `direction_cycle % len(...)` 取模；防回归 UT `test_direction_cycle_no_reversal`（两脚本各一，断言相邻项含首尾互不为反向）已加入并通过。
  3. **MTO 复测标准实测**：`bench_memory --duration 3`（此前必崩 AssertionError@app.py:618）现 PASS（峰值 57.8MB ≤ 300MB），`--duration 10` PASS（57.5MB）；`bench_fps --duration 3` PASS（P95 0.60ms ≤ 25ms、avg FPS 1710 ≥ 60），`--duration 10` PASS（P95 0.63ms）。NFR-01/02 均可实测，退出码 0。
  4. **结论**：主缺陷 + 连带缺陷（NFR-01/02 影响面）全部修复并经真实执行验证，无遗留问题。close。
- 2026-08-15T12:19:21Z 提单人复测通过，关闭
