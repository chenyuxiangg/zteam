# 模块测试报告：game-app 迭代 2

## 结论：PASS

## 覆盖范围

- 用例：IT-game-app-2-01 ~ IT-game-app-2-33（**36 个测试函数 + 3 个参数化变体 = 39 个测试用例**，TE 已评审 PASS）
  - P0：23 条（暂停/失焦/最高分联动/状态机/集成端到端/退出码）
  - P1：16 条（接口契约/HUD 数据源/最高分展示/渲染分发/R3 修订项回归）

> 备注：部分用例 ID（IT-game-app-2-03、02、11、10）拆为多个测试函数（pause/unfocus/MOVE_* 参数化等），故函数总数 36 > 文档中 33 条基线 ID，但与用例文档 §1 矩阵 1:1 对应（每条 ID 至少一个测试函数覆盖）。

## 执行结果

```
============================== 39 passed in 0.59s ==============================
```

测试代码：`snake-linux/it/game-app/iter-2/test_it_game_app_2.py`
junitxml 报告：`snake-linux/it/game-app/iter-2/it-report.xml`
用例文档：`snake-linux/it/game-app/iter-2/测试用例.md`

## 需求 → 用例覆盖

| 需求 | 用例 |
|------|------|
| FR-01（玩法闭环：移动/吃食/得分） | IT-game-app-2-11（INV-4 pending 合并）/20/30（端到端得分回调触发） |
| FR-04（结束判定：撞墙/撞身/OVER 保护） | IT-game-app-2-13（OVER→toggle_pause 抛 InvalidStateError）/30（端到端撞墙→GAME_OVER） |
| FR-05（难度选择入口，iter-2 不重写 UI） | IT-game-app-2-01（MENU 态 SELECT_*；PLAYING 不可改 INV-3） |
| FR-11（开始/重开/退出/返回菜单） | IT-game-app-2-06（ESC 独立 action，P1-2）/08（Backspace→BACK_TO_MENU，G2-7）/09（GAME_OVER Q→QUIT 直通，R3-7）/30（端到端完整链路）/32（主循环退出码 0 + shutdown 兜底，R3-15） |
| FR-12（暂停/继续 + 失焦自动暂停） | IT-game-app-2-02（P/UNFOCUS/恢复不自动）/10（PAUSED 态 MOVE_*/UNFOCUS 忽略；TOGGLE→PLAYING）/11（PLAYING 态 TOGGLE/UNFOCUS→PAUSED，方案 A INV-11）/12（_run_loop 跳过 PAUSED 态 _tick，INV-10）/13（OVER 态 toggle_pause 抛 InvalidStateError）/14（_render PAUSED 路径）/15（draw_pause_overlay 内容）/26（PAUSED→PLAYING 累加器归零，R3-8） |
| FR-13（最高分持久化 + 展示/重置） | IT-game-app-2-03（_init_pygame + H 键 + 回调）/16（构造 HighScoreStore）/17（mkdir 失败包 AppError，P1-1）/18（H 键 RESET）/19（storage.reset 抛 StorageError 包 StorageUnavailableError）/20（回调内同步 _high_score + storage.save，INV-13 P0-2）/21（_storage=None 时 callback=None，UT 隔离）/22（回调内 save 抛 StorageError 包 StorageUnavailableError）/23（draw_menu high_score 条件绘制）/24（draw_game_over 高分 + 返回菜单提示，G2-7）/25（HUD 高分字段数据源 INV-13）/31（真实 HighScoreStore IO，tmp_path 隔离） |
| NFR-01（节拍累加 + 循环内重读 tick_ms） | IT-game-app-2-12（PAUSED 跳过 tick，INV-10）/26（PAUSED→PLAYING 累加器归零，R3-8） |
| NFR-03（错误提示最小集） | IT-game-app-2-17（mkdir 失败 AppError 含"用户数据目录不可写"）/19/22（StorageError 包装为可读 StorageUnavailableError） |
| NFR-05（Python 3.8 兼容 + 职责分离） | IT-game-app-2-33（iter-1 R3 修订项无回归：构造无副作用 R3-10、屏态兜底 R3-1、死代码清理 R3-7、退出码 2 兜底 R3-15） |

## 集成契约验证（G2 修订项专项）

| 修订 | 验证用例 |
|------|----------|
| **G2-1 PAUSED 状态机扩展** | 02/10/11/12/13/14/15/26（屏态同步方案 A 单点保证 INV-11；INV-10 PAUSED 跳过 tick；TOGGLE_PAUSE dispatch 内显式切屏） |
| **G2-2 HighScoreStore 接入** | 03/16/17/31（_storage 默认 None 让 UT 不依赖磁盘；mkdir 失败包 AppError；tmp_path 真实 IO 验证） |
| **G2-3 得分回调同步 _high_score + storage.save** | 03/20/21/22（INV-13 P0-2：回调内直接写 self._high_score 实例字段；storage.save 抛错包装 StorageUnavailableError） |
| **G2-4 窗口失焦自动暂停** | 02/07/11（仅 PLAYING 态失焦追加 UNFOCUS；UNFOCUS 是内部信号 _map_event 不产生；恢复不自动） |
| **G2-5 暂停遮罩** | 14/15（renderer.render 最后一帧 + draw_pause_overlay 叠加；pygame.Surface(SRCALPHA) + 居中文字 + 走 pygame.display.get_surface 不读 renderer 私有） |
| **G2-6 最高分展示** | 23/24/25（draw_menu / draw_game_over 形参 high_score >0 才绘；HUD 数据源 = self._high_score） |
| **G2-7 返回菜单** | 06/08/24（Backspace 直返 BACK_TO_MENU 不经屏态覆盖；ESC 经 GAME_OVER 屏态覆盖；GAME_OVER 提示含 Esc/Backspace） |
| **P1-2 ESC 独立 action 与 Q 区分** | 06/09（_map_event(K_ESCAPE)→ESCAPE 不返 QUIT；GAME_OVER 态 _drain_events 覆盖为 BACK_TO_MENU；Q 始终 QUIT 直通） |
| **P1-3 fixture 注入 fake_storage 覆盖** | 03/18/31（_storage is None 时 create_storage；已注入则跳过） |

## 端到端集成路径（IT-game-app-2-30）

MENU → PLAYING（K_RETURN）→ PAUSED（K_p）→ PLAYING（K_p）→ 触发回调 cb(75) 验证 _high_score=75 + storage.save(75) → 强制 OVER → GAME_OVER → K_BACKSPACE → MENU（game_state=None INV-7 重置）

全部状态机迁移与屏态同步方案 A 单点保证均通过。

## 真实 IO 验证（IT-game-app-2-31）

- tmp_path 注入 HighScoreStore：`save(50) → load()=50`；文件落盘 `highscore.json` 存在
- 重新构造（同路径）：`s2.load() == 50`（持久化有效）
- `_dispatch_menu(RESET_HIGHSCORE)`：`storage.load()=0`；文件被 unlink

## iter-1 R3 修订项回归（IT-game-app-2-33，无回归）

- R3-1 屏态兜底：MENU 态 K_RETURN 未映射 → actions 含 START ✓
- R3-10 构造无副作用：`_renderer is None and _storage is None and game_state is None and fake_pygame.init.call_count == 0` ✓
- R3-7 死代码清理：`hasattr(app, "_quit") == False` ✓
- R3-15 退出码 2 兜底：`display.set_mode.side_effect = RuntimeError → run() == 2` ✓

## 缺陷（问题单）

- 无（39 条全部通过，0 问题单 open）

## 关键验证点

1. **屏态同步方案 A（INV-11）单点保证**：`_dispatch_playing(TOGGLE_PAUSE/UNFOCUS)` 与 `_dispatch_paused(TOGGLE_PAUSE)` 内显式赋值 `self.screen`，与 §3.4 docstring + §1.4 不变量三方对齐
2. **INV-13 P0-2 回调同步实例字段**：`_new_game` 注册的回调 `lambda s: self._on_score(s)` 内直接写 `self._high_score = max(...)`，不再用 nonlocal/外部容器
3. **P1-1 mkdir 失败双类型捕获**：`except (StorageError, OSError)`（与 `platform-storage` iter-2 实际抛 OSError 对齐）
4. **GAME_OVER 屏态覆盖仅作用于 ESC**：Backspace 由 `_map_event` 直返 BACK_TO_MENU（不经 `_drain_events` 覆盖）；Q 始终 QUIT 直通（主循环 break）
5. **暂停遮罩走 R3-2 不读 renderer 私有**：`pygame.display.get_surface()` 拿 surface，半透明 overlay 由 `pygame.Surface(surface.get_size(), pygame.SRCALPHA)` 独立构造

## 文件清单

| 路径 | 角色 | 状态 |
|------|------|------|
| `snake-linux/it/game-app/iter-2/测试用例.md` | MTO 用例文档 | TE 评审 PASS（`release_module case PASS` 已执行） |
| `snake-linux/it/game-app/iter-2/test_it_game_app_2.py` | IT 测试代码（39 用例） | 全部 PASS |
| `snake-linux/it/game-app/iter-2/it-report.xml` | pytest junitxml | 39 passed |
| `snake-linux/it/game-app/iter-2/模块测试报告.md` | 本报告 | — |