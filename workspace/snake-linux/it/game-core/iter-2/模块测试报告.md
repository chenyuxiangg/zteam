# 模块测试报告：game-core 迭代 2

## 结论：PASS

## 覆盖范围

- 用例：IT-game-core-2-01 ~ IT-game-core-2-26（共 26 条，TE 已评审 PASS）
  - P0：17 条（FR-05/12/13 核心功能 / NFR-01 量化 / 回归锚点）
  - P1：9 条（边界 / 替换回调 / 单一数据源验证等）
- 执行：通过 26 / 失败 0 / 跳过 0

## 执行结果

```
============================== 26 passed in 0.07s ==============================
```

测试代码：`workspace/snake-linux/it/game-core/iter-2/test_it_game_core_2.py`
junitxml 报告：`workspace/snake-linux/it/game-core/iter-2/it-report.xml`
覆盖率：行 93% / 分支 84%（`state.py` 91% / `params.py` 100% / `types.py` 98%）

## 需求 → 用例覆盖

| 需求 | 用例 |
|------|------|
| FR-05（难度分级 + 三档节拍量化） | IT-game-core-2-08/09/10/11/12/25 |
| FR-12（暂停/继续：定格 + 方向输入不生效 + OVER 抛错 + INV-8 清 pending） | IT-game-core-2-01/02/03/04/05/06/07 |
| FR-13（得分事件入口：触发/不触发/None 静默/异常不捕获/替换/多次） | IT-game-core-2-14/15/16/17/18/19/22/26 |
| NFR-01（性能：默认节拍 160ms / HARD ≤ EASY*0.5 / 三档独立下限） | IT-game-core-2-08/09/11/13 |
| FR-05/12（接口契约：speed_curve → Snapshot.tick_ms / base_tick_ms） | IT-game-core-2-12/13 |
| FR-05/12/13（回归锚点：iter-1 接口契约与核心行为不变） | IT-game-core-2-20/21 |
| NFR-05（零 GUI 依赖 + Python 3.8 语法兼容） | IT-game-core-2-23/24 |

## 缺陷（问题单）

- 无（26 条全部通过，0 问题单 open）

## 关键验证点

### 增量三件套 1：speed_curve 加速曲线（NFR-01 量化）

- **score=0 三档基线**：`speed_curve(0, EASY/MEDIUM/HARD) == 250/160/100` ✅
- **NFR-01 量化（任意 score 下 HARD ≤ EASY*0.5）**：对 score∈[0,100] 全部断言通过 ✅
- **单调不增**：score 增大、tick_ms 不增（三档 subTest） ✅
- **三档独立下限**：score=100 时 EASY=100/MEDIUM=80/HARD=50；score=10000 仍钳制 ✅
- **单一数据源**：`Difficulty.base_tick_ms` 走 `speed_curve(0, self)`；`Snapshot.tick_ms` 走 `speed_curve(score, difficulty)` ✅
- **MIN_TICK_MS 单一数据源**：修改 dict 后 speed_curve 立即反映新值（验证后还原） ✅

### 增量三件套 2：toggle_pause 暂停状态机

- **RUN→PAUSED 字段冻结（INV-9）**：除 status 外所有字段不变（蛇身/食物/得分/长度/方向/难度） ✅
- **PAUSED→RUN 清 pending（INV-8）**：恢复时 `pending_direction=None` ✅
- **OVER 不可暂停**：`toggle_pause(OVER)` 抛 `InvalidStateError` ✅
- **PAUSED 期 step 抛错**：`InvalidStateError`（含 PAUSED/OVER） ✅
- **PAUSED 期 set_direction 静默忽略**：不入 pending、不抛错 ✅
- **INV-8 核心防呆**：暂停前按 UP → 暂停 → 继续 → 第一拍按原 direction=RIGHT 走（非 UP） ✅
- **反复 toggle_pause 等价**：4 次翻转后 status=RUN、字段全不变 ✅

### 增量三件套 3：on_score 得分回调

- **触发时机**：step 吃食时调 `cb(new_score)`；非吃食不触发 ✅
- **回调参数 = new_score**：每次触发收到当次 new_score ✅
- **回调为 None 静默**：score 字段仍正确更新 ✅
- **回调异常不捕获 + pure-function 语义**：`RuntimeError` 向外抛、旧 state 未污染（`s.snapshot() == before`） ✅
- **回调替换**：`set_score_callback` 后新 cb 生效、旧 cb 不再被调 ✅
- **构造期注入等价**：构造时 score_callback 与 set_score_callback 行为一致 ✅
- **回调清空**：传 None 后 step 吃食不触发 ✅

### 回归锚点

- **iter-1 接口契约不变**：`Point/Direction/Difficulty/GameStatus/Snapshot` 公开字段名/类型/可构造性保留；`Snapshot.tick_ms` 字段名不变（语义从 `base_tick_ms` 改为 `speed_curve(score, difficulty)`，属非破坏性变更） ✅
- **iter-1 核心行为不变**：移动/吃食/反向禁止/撞墙/撞身/撞尾让行/OVER 保护全部回归通过 ✅
- **iter-1 32 条用例**：在 iter-1 代码下仍全部通过（保留为历史回归基线，不在 iter-2 IT 中重复运行）

### 静态检查（NFR-05）

- **零 GUI 依赖**：game_core 全部 5 个文件（types/params/state/errors/__init__）无 pygame/pyinstaller/tkinter/PyQt/wx 等 import ✅
- **Python 3.8 语法兼容**：AST 解析全部通过，扫描无 PEP 604（`X | None`）/ 内置泛型下标（`tuple[X,...]`/`dict[K,V]`） ✅

## 通过判定

- P0 全部 PASS（无 skip、无 fail）→ 模块迭代 PASS
- 全部问题单闭环（本次无 open）
- 模块进入 `it_passed` 状态（`release_module ... it ... DONE` 已执行）
