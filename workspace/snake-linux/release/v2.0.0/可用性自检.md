# 可用性自检：snake-linux v2.0.0

> QA 发布前冒烟记录 · 2026-08-15T12:46Z · 真实执行（非编造）

## 环境

| 项 | 值 |
|----|----|
| 平台 | Linux（Ubuntu 兼容环境） |
| Python | 3.11.15 |
| pygame | 2.6.1（SDL 2.28.4） |
| 驱动 | SDL dummy（headless）+ SDL_AUDIODRIVER=dummy |
| 被测代码 | game-core/iter-2 + platform-storage/iter-2 + gui-renderer/iter-3 + game-app/iter-4 |

## 冒烟结果（7/7 通过）

| # | 检查项 | 结果 | 证据 |
|---|--------|------|------|
| 1 | 四包可导入 | PASS | game_core/platform_storage/gui_renderer/game_app 均 import 成功 |
| 2 | App 构造 + _init_pygame | PASS | screen=AppScreen.MENU |
| 3 | 开局 EASY + 首帧渲染 | PASS | tick_ms=250（难度参数表穿透 app） |
| 4 | 节拍推进 + 渲染 | PASS | 250ms → 1 拍，status=GameStatus.RUN |
| 5 | 3 皮肤真实渲染（MTO-4-01 修复面） | PASS | classic/dark/colorblind_friendly 逐一切换 + _render 无异常 |
| 6 | 进程级 `python -m game_app` 启动存活 | PASS | 启动 3s 后 poll=None（菜单态，未崩溃） |
| 7 | 进程可干净终止 | PASS | terminate 后 returncode=0 |

## 源码发布包复验

- `snake-linux-v2.0.0.tar.gz` 解压后独立运行：`App(AppConfigV3())` + `_init_pygame` + `_new_game(MEDIUM)` + `_tick(160)` + `_render` → **PACKAGE_RUN_OK screen=AppScreen.PLAYING tick_ms=160**（说明打包内容自洽、可脱离工作区运行）。

## 关键修复面复核

- **MTO-4-01（P0，Color dataclass → pygame TypeError）**：真实 pygame 2.6.1 下逐皮肤真实渲染（冒烟 #5）无 TypeError，修复生效。
- **MTO-4-01 连带缺陷（bench 方向序列反向项 → 撞墙断言崩溃）**：`scripts/bench_fps.py` / `bench_memory.py` 方向序列已改为无反向项循环（`BENCH_DIRECTION_CYCLE`），由 MTO 复测 close 确认（bench `--duration 3/10` 均 PASS）。

## 结论

**可用性自检 PASS**：四模块真实装配成立、源码模式启动链路成立、渲染闭环（含 3 皮肤）无异常、进程干净退出、发布包内容可独立运行。
