  ┊ review diff
a/workspace/snake-linux/design/game-core/设计-r1.md → b/workspace/snake-linux/design/game-core/设计-r1.md
@@ -0,0 +1,513 @@
+# 功能模块设计：game-core（snake-linux v2.0.0 迭代 1）
+
+> MDE 产出 · 对齐架构 `snake-linux/arch/v2.0.0/架构设计.md` 与分工表 `功能模块分工表.md`
+> 迭代 1 范围：玩法闭环（移动/吃食/碰撞/反向禁止/得分/状态机 run+over）+ 难度参数表（EASY/MEDIUM/HARD 一次到位，**无加速曲线**——加速曲线放迭代 2 与暂停一同落地）
+> 迭代 2 增量：暂停状态机、speed_curve(score)、on_score 事件
+> **零 GUI 依赖、纯标准库、可独立 UT**（NFR-05）
+
+---
+
+## 数据结构
+
+### 1.1 基础值对象
+
+| 类型 | 字段 | 说明 |
+|------|------|------|
+| `Point`（值对象） | `x: int`、`y: int` | 网格坐标。y 向下、0,0 为左上角，沿用 v1 约定 |
+| `Direction`（枚举） | `UP=(0,-1)` / `DOWN=(0,1)` / `LEFT=(-1,0)` / `RIGHT=(1,0)` | 4 向；`opposite()` 工具方法返回反向 |
+| `GameStatus`（枚举） | `RUN` / `PAUSED` / `OVER` | 运行态机；迭代 1 仅 RUN/OVER，PAUSED 枚举先占位但不允许进入 |
+| `Difficulty`（枚举） | `EASY` / `MEDIUM` / `HARD` | 难度档位，绑定 `base_tick_ms` |
+
+### 1.2 难度参数表
+
+```python
+DIFFICULTY_PARAMS = {
+    Difficulty.EASY:   {"base_tick_ms": 250},
+    Difficulty.MEDIUM: {"base_tick_ms": 160},
+    Difficulty.HARD:   {"base_tick_ms": 100},
+}
+```
+
+> 迭代 1 仅落 base_tick_ms。加速曲线（`speed_curve(score) -> tick_ms`）放迭代 2；迭代 1 内部不出现 score→tick 的换算逻辑，UI 层直接用 `base_tick_ms`。
+
+### 1.3 核心实体
+
+**`Snake`**
+- 字段：`body: tuple[Point, ...]`（**不可变 tuple**，蛇头 = `body[0]`，蛇尾 = `body[-1]`）
+- 不变量：`len(body) >= 1`；相邻两节点必须 4-邻接
+- 派生：`head -> Point`、`len -> int`
+- 关键方法（生成新对象，不修改自身）：
+  - `with_head(new_head) -> Snake`：在头部插入新节点（移动未吃食）
+  - `without_tail() -> Snake`：去掉尾部（移动未吃食，与 `with_head` 配对）
+  - `with_head_no_tail_drop(new_head) -> Snake`：仅头部插入（吃食后不丢尾）
+  - `contains(p) -> bool`：判断点是否在蛇身
+
+**`Food`**
+- 字段：`pos: Point`
+- 不变量：`pos not in snake.body`
+
+**`GameState`**
+- 字段：
+  - `width: int`（网格宽，列数；建议默认 20）
+  - `height: int`（网格高，行数；建议默认 15）
+  - `difficulty: Difficulty`
+  - `snake: Snake`（初始 3 节，居中朝右：body=[(W//2, H//2), (W//2-1, H//2), (W//2-2, H//2)]）
+  - `direction: Direction`（初始 RIGHT）
+  - `pending_direction: Direction | None`（**同帧多输入合并用**：每个 step 取一次最终方向，规避一帧内连按 WASD 跳过反向的漏洞）
+  - `food: Food`
+  - `score: int`（**每节长度+1 = 1 分**；迭代 1 不引入复杂计分）
+  - `status: GameStatus`
+  - `rng: random.Random`（注入，便于 UT 确定性，**禁止隐式全局 random**）
+  - `_last_step: int | None`（节拍序号，用于事件订阅；迭代 1 可不暴露）
+
+> 迭代 1 不引入 `events` 队列、`on_score` 回调（事件机制放迭代 2 与 `platform-storage` 接入时一起做）；得分判定由调用方读 `state.score` 即可。
+
+### 1.4 不变量清单（FO 实现必须保证，UT 也要覆盖）
+
+| ID | 不变量 | 违反后果 |
+|----|--------|----------|
+| INV-1 | `len(snake.body) >= 1` 且相邻节点 4-邻接 | 移动逻辑破坏 |
+| INV-2 | `food.pos not in snake.body` | 同位 bug |
+| INV-3 | `0 <= head.x < width` 且 `0 <= head.y < height` | 出界 bug（撞墙判定在 step 入口先做） |
+| INV-4 | `status == OVER` 后所有 step/set_direction 不再改变 snake/food/score | 终态保护 |
+| INV-5 | `pending_direction` 若非 None，则与 `direction` 非 opposite（受理时校验） | 反向移动禁止 |
+| INV-6 | `rng` 始终是注入实例，模块内不出现 `import random` 后直接 `random.xxx` | 确定性测试前提 |
+
+---
+
+## 数据传递方式
… omitted 435 diff line(s) across 1 additional file(s)/section(s)

session_id: 20260814_003121_5abe92
状态已正确从 `design_working` 推进到 `design_reviewing`，等待 SE 评审。设计文档落盘于 `snake-linux/design/game-core/设计-r1.md`，按 MDE 模板覆盖：数据结构（6 条不变量）、数据传递方式（注入 + 不可变快照）、对外接口（8 个公开 API + 异常）、实现细节（5 步 step 流程、文件组织、7 条实现注意）、DFx/可测试性/鲁棒性、CPU/内存/依赖资源评估、UT 框架（20 个必写用例 + 95% 覆盖率门槛 + RED-GREEN-REFACTOR 步骤）。
  ┊ review diff
a/workspace/snake-linux/design/game-core/设计-r2.md → b/workspace/snake-linux/design/game-core/设计-r2.md
@@ -0,0 +1,607 @@
+# 功能模块设计：game-core（snake-linux v2.0.0 迭代 1）
+
+> MDE 修订产出 r2 · 对齐架构 `snake-linux/arch/v2.0.0/架构设计.md` 与分工表 `功能模块分工表.md`
+> 修订依据：SE 评审 `review/design/game-core/iter-1/snake-linux-game-core-design-iter1-r1.md`（FAIL）
+> 迭代 1 范围：玩法闭环（移动/吃食/碰撞/反向禁止/得分/状态机 run+over）+ 难度参数表（EASY/MEDIUM/HARD 一次到位，**无加速曲线**——加速曲线放迭代 2 与暂停一同落地）
+> 迭代 2 增量：暂停状态机、speed_curve(score)、on_score 事件
+> **零 GUI 依赖、纯标准库、可独立 UT**（NFR-05）
+
+---
+
+## 修订摘要（相对 r1）
+
+| ID | 级别 | 修订内容 |
+|----|------|----------|
+| P1-1 | 阻塞 | 接口注解改 Python 3.8 兼容：`Optional[X]` / `Tuple[Point, ...]`，from typing 引入 |
+| P1-2 | 阻塞 | 撞自身判定统一为 v1 行为：`new_head in set(body) and not (new_head == 旧尾 and not 吃食)`，§3.2 注释二合一，UT #6 改写 |
+| P2-1 | 应修订 | 最小网格校验改为 `width >= 4` 且 `height >= 4`（初始 3 节水平蛇需要至少 4 列宽度），UT #14/#14b 同步 |
+| P2-2 | 应修订 | `set_direction` 显式裁决：**实现长度 1 特例**（与架构约束一致，初始蛇长 3 长度 1 实际不可达但契约必须覆盖）；§3.2 + UT #9 同步 |
+| P3-2 | 建议 | `base_tick_ms` 改为从 `DIFFICULTY_PARAMS[difficulty]["base_tick_ms"]` 读取（property 实现走单一数据源，删除冗余 property 内部硬编码） |
+| P3-3 | 建议 | 测试组织统一为 **unittest**（setUp 内 `self.rng = random.Random(42)`），pytest 仅作可选（CI 兜底统一用 `python3 -m unittest`） |
+
+P3-1 已由 SE 裁决放行（frozen dataclass 保留），未在本修订改动。
+
+---
+
+## 数据结构
+
+### 1.1 基础值对象
+
+| 类型 | 字段 | 说明 |
+|------|------|------|
+| `Point`（值对象） | `x: int`、`y: int` | 网格坐标。y 向下、0,0 为左上角，沿用 v1 约定 |
+| `Direction`（枚举） | `UP=(0,-1)` / `DOWN=(0,1)` / `LEFT=(-1,0)` / `RIGHT=(1,0)` | 4 向；`opposite()` 工具方法返回反向 |
+| `GameStatus`（枚举） | `RUN` / `PAUSED` / `OVER` | 运行态机；迭代 1 仅 RUN/OVER，PAUSED 枚举先占位但不允许进入 |
+| `Difficulty`（枚举） | `EASY` / `MEDIUM` / `HARD` | 难度档位，绑定 `base_tick_ms`（迭代 2 起改走 `speed_curve`） |
+
+### 1.2 难度参数表（单一数据源）
+
+```python
+from typing import Dict
+from enum import Enum
+
+class Difficulty(Enum):
+    EASY = "easy"
+    MEDIUM = "medium"
+    HARD = "hard"
+
+
+# 单一数据源：迭代 2 加 speed_curve(score) 时仅替换本 dict 的内容/形态
+DIFFICULTY_PARAMS: Dict[Difficulty, Dict[str, int]] = {
+    Difficulty.EASY:   {"base_tick_ms": 250},
+    Difficulty.MEDIUM: {"base_tick_ms": 160},
+    Difficulty.HARD:   {"base_tick_ms": 100},
+}
+```
+
+> 迭代 1 仅落 `base_tick_ms`。加速曲线（`speed_curve(score) -> tick_ms`）放迭代 2；迭代 1 内部不出现 score→tick 的换算逻辑，UI 层直接用 `difficulty.base_tick_ms`。
+
+### 1.3 核心实体
+
+**`Snake`**
+- 字段：`body: Tuple[Point, ...]`（**不可变 tuple**，蛇头 = `body[0]`，蛇尾 = `body[-1]`）
+- 不变量：`len(body) >= 1`；相邻两节点必须 4-邻接
+- 派生：`head -> Point`、`len -> int`
+- 关键方法（生成新对象，不修改自身）：
+  - `with_head(new_head) -> Snake`：在头部插入新节点（移动未吃食）
+  - `without_tail() -> Snake`：去掉尾部（移动未吃食，与 `with_head` 配对）
+  - `with_head_no_tail_drop(new_head) -> Snake`：仅头部插入（吃食后不丢尾）
+  - `contains(p) -> bool`：判断点是否在蛇身
+
+**`Food`**
+- 字段：`pos: Point`
+- 不变量：`pos not in snake.body`
+
+**`GameState`**
+- 字段：
+  - `width: int`（网格宽，列数；建议默认 20）
+  - `height: int`（网格高，行数；建议默认 15）
… omitted 529 diff line(s) across 1 additional file(s)/section(s)

session_id: 20260814_011131_c3282f
修订完成，状态已迁移到 `design_reviewing` 等待 SE 复评。
