  ┊ review diff
a/workspace/snake-linux/design/game-app/设计-r1.md → b/workspace/snake-linux/design/game-app/设计-r1.md
@@ -0,0 +1,763 @@
+# 功能模块设计：game-app（snake-linux v2.0.0 迭代 1）
+
+> MDE 首发产出 · 严格对齐架构 `snake-linux/arch/v2.0.0/架构设计.md`、分工表 `snake-linux/arch/v2.0.0/功能模块分工表.md`、需求规格 `snake-linux/analysis/snake-gui-r1.md`（R-01~R-09 用户拍板已固化）
+> 依赖模块：game-core（迭代 1 已 it_passed）、gui-renderer（迭代 1 已 it_passed）、platform-storage（迭代 2 接入，本迭代不调用）
+> **迭代 1 范围 = 最小时可玩闭环**（主循环 + 输入 + 开始/结束/重开/退出 + 难度选择最小集），对齐分工表 §迭代计划出口：FR-01~04 + FR-11（开始/重开/退出）+ FR-05 难度选择入口
+> **迭代 2/3/4 增量**（本设计**不实现**，仅留接口与扩展点预告）：暂停/继续、最高分展示/重置（FR-12/FR-13）、皮肤切换 UI（FR-10）、三平台打包（FR-14/15）、用户指南（FR-16）、可读错误提示（NFR-03）、性能调优（NFR-01/02）
+> **目标**：FO 拿到本文即可 TDD 开发，UT 框架明确
+
+---
+
+## 0. 模块定位与迭代边界
+
+| 项 | 值 |
+|----|---|
+| 模块 | game-app |
+| 类型 | 上层应用 |
+| 依赖 | game-core（纯逻辑，零 GUI）、gui-renderer（pygame 渲染，迭代 1 已 it_passed）、platform-storage（迭代 2 接入） |
+| 被依赖 | 无（顶层装配） |
+| 承载需求 | snake-gui **主体**（FR-01~16 中除 gui-renderer 子集外的全部）—— 本迭代 1 范围 = FR-01~05 玩法闭环入口 + FR-11 开始/结束/重开/退出 |
+| 迭代 | 1（首发） |
+| 不引入 | 第三方除 pygame 外任何依赖；不引入音效（架构 §R-04）；不引入网络（NFR-06）；不引入 config 文件（架构 §配置模型）；不写系统目录（便携式，NFR-07） |
+| 跨迭代复用 | 主循环骨架 / 界面状态机 / 输入映射 / 错误处理框架 跨 4 迭代复用；迭代 2/3 通过**新增状态节点与处理函数**接入，不重写主循环 |
+| PyInstaller 入口 | `snake-gui.py`（包根 `__main__.py`，`if __name__ == "__main__": main()`） |
+
+### 迭代 1 出口（与架构 §迭代计划 对齐）
+
+- ✅ 主事件循环（pygame 帧驱动，clock.tick 60FPS）
+- ✅ 输入映射（WASD/方向键 → core.set_direction；P/ESC 迭代 1 仅占位为"按 P 显示 'Pause (iter 2)' 提示文字"；Q 退出）
+- ✅ 界面状态机：`MENU` → `PLAYING` → `GAME_OVER` →（重开 → `PLAYING` / 退出）
+- ✅ 开始界面：标题 + "按任意键开始" + 难度选择（EASY/MEDIUM/HARD 通过 1/2/3 键 + 方向键 + Enter 确认）
+- ✅ 游戏 HUD：得分 + 长度（最高分占位"---"，迭代 2 接入）
+- ✅ 结束画面：最终得分 + "重开 (R) / 退出 (Q)"
+- ✅ 退出 1 秒内无残留进程（FR-11，pygame.quit() + sys.exit 显式调用）
+- ✅ 帧率 ≥ 60 FPS（pygame clock 兜底）
+- ❌ 暂停/继续（FR-12，迭代 2）
+- ❌ 最高分持久化（FR-13，迭代 2）
+- ❌ 皮肤切换 UI（FR-10，迭代 3）
+- ❌ 窗口缩放（FR-09，迭代 3）
+- ❌ 平滑动画（FR-07，迭代 3）
+- ❌ 三平台打包（FR-14/15，迭代 4）
+- ❌ 完善错误提示（NFR-03，迭代 4）
+
+### 迭代 1 已知技术约束（FO 实现必读）
+
+1. **Python 3.8 兼容**：与架构 §代码风格约定一致，不使用 dataclass 自定义 `__setattr__` 之外的 3.9+ 特性；`Optional[X]` / `Tuple[X, ...]` 而非 `X | None` / `tuple[X, ...]`。
+2. **零配置**：不读 ini/env/YAML/JSON 配置；难度/皮肤通过游戏内 UI 选择（皮肤迭代 1 仅经典单套，迭代 3 扩展）。
+3. **无网络**：全模块不 `import socket` / `import urllib` / `import http` / `import requests`；UT 不发起网络（mocket 都不需要）。
+4. **无音效**：不 `import pygame.mixer` 或任何音频模块。
+5. **依赖边界**：game-app **可** import pygame（用于事件循环与窗口）；**不可**侵入 game-core 内部（仅通过 game-core 的公开 API：`Point`/`Direction`/`Difficulty`/`GameStatus`/`GameState`/`Snapshot`/`InvalidStateError`）；**不可**直接改 gui-renderer 内部状态（通过 `Renderer(skin_name).render(snapshot)` 调用）。
+6. **platform-storage 迭代 1 不导入**：`import platform_storage` 不出现在迭代 1 代码中；最高分变量名 `high_score` 留位但赋值为 `0` 占位，迭代 2 接入。
+
+---
+
+## 数据结构
+
+### 1.1 界面状态枚举（app 层级，独立于 core 的 GameStatus）
+
+| 类型 | 字段 | 说明 |
+|------|------|------|
+| `AppScreen`（Enum） | `MENU` / `PLAYING` / `GAME_OVER` | app 顶层界面状态机；迭代 1 三态；`PAUSED` 枚举先占位但不允许进入（迭代 2 接入） |
+| `InputAction`（Enum） | `QUIT` / `START` / `MOVE_UP` / `MOVE_DOWN` / `MOVE_LEFT` / `MOVE_RIGHT` / `TOGGLE_PAUSE` / `RESTART` / `SELECT_EASY` / `SELECT_MEDIUM` / `SELECT_HARD` | 输入归一化结果，pygame 事件 → InputAction 映射（详见 §3.3） |
+| `AppConfig`（dataclass, frozen） | `window_w: int = 800` / `window_h: int = 600` / `fps_cap: int = 60` / `min_window_w: int = 480` / `min_window_h: int = 360` | 不可变运行期常量；迭代 1 硬编码默认值，迭代 3 接入窗口缩放时再扩字段 |
+
+### 1.2 难度选择状态（菜单内子状态）
+
+| 类型 | 字段 | 说明 |
+|------|------|------|
+| `MenuCursor`（Enum） | `START` / `EASY` / `MEDIUM` / `HARD` / `QUIT` | 菜单高亮项；↑/↓ 移动，Enter 确认 |
+| `_selected_difficulty: Difficulty` | `class 内部 state` | 菜单态当前选中难度（默认 `Difficulty.MEDIUM`），按 Enter 后写入新 `GameState` |
+
+### 1.3 运行期状态（app 内部 mutable state，不暴露给 core）
+
+| 字段 | 类型 | 说明 |
+|------|------|------|
+| `screen: AppScreen` | 当前界面 | 初始 `MENU` |
+| `difficulty: Difficulty` | 当前局难度 | 初始 `MEDIUM`；从菜单选好后固化到 `GameState`（**游戏中不可改**，对齐 FR-05） |
+| `game_state: GameState` | 玩法状态 | 首次开局时由 `_new_game(difficulty)` 构造；`GAME_OVER` 时保留展示 |
+| `renderer: Renderer` | 渲染器 | 迭代 1 构造时皮肤固定 `"classic"`；迭代 3 接受 `skin_name` 参数 |
… omitted 685 diff line(s) across 1 additional file(s)/section(s)

session_id: 20260814_024047_72049d
完成。设计文档落盘 + 状态机迁移 design_working → design_reviewing，审计行已写入 pipeline.log（MODULE_DESIGN 2026-08-14T02:47:55Z）。
  ┊ review diff
a/workspace/snake-linux/design/game-app/设计-r2.md → b/workspace/snake-linux/design/game-app/设计-r2.md
@@ -0,0 +1,966 @@
+# 功能模块设计：game-app（snake-linux v2.0.0 迭代 1）r2（修订版，回应 SE 评审 FAIL）
+
+> MDE r2 · 修订版 · 严格对齐架构 `snake-linux/arch/v2.0.0/架构设计.md`、分工表 `snake-linux/arch/v2.0.0/功能模块分工表.md`、需求规格 `snake-linux/analysis/snake-gui-r1.md`（R-01~R-09 已固化）
+> 依赖模块实际契约：game-core **迭代 2**（`code/game-core/iter-2/game_core/` — dev_reviewing，本设计 **以迭代 2 接口为准**，落定后即时同步验契约）、gui-renderer **迭代 1**（`code/gui-renderer/iter-1/gui_renderer/` — it_passed，**契约已锁定**）、platform-storage（迭代 2 接入，本迭代不调用）
+> r1 评审：`snake-linux/review/design/game-app/iter-1/snake-linux-game-app-design-iter1-r1.md`（FAIL）
+> r2 修订要点（响应 SE 评审 6×P0 + 7×P1/P2）：见下表 + §附录 D 修订对照
+> **目标**：FO 拿到本文即可 TDD 开发，UT 框架明确；按"依赖模块已落地代码"做契约核对（不再纸面对纸面）
+
+---
+
+## 修订摘要（响应 SE 评审 FAIL）
+
+| ID | 级别 | 修订内容 | 章节 |
+|----|------|----------|------|
+| R2-1 (P0-1) | 应实现 | Renderer 构造签名：`Renderer((W, H), skin=DEFAULT_SKIN)` 二元组传入，**不再用字符串 "classic"** | §3.4 / §4.7 / §6.4 |
+| R2-2 (P0-2) | 应实现 | Renderer 生命周期：`Renderer.init()` 由主循环构造后调一次（或 `with Renderer(...) as r` 上下文）；窗口职责唯一交给 `renderer.init()`，app 不再单独 `display.set_mode`；**删除**对 `set_skin` 调用（迭代 1 不存在） | §4.7 / §5.3 |
+| R2-3 (P0-3) | 应实现 | HUD 契约：`HudData(score:int, high_score:int, length:int, difficulty_label:str, status_label:str)` 5 字段 dataclass（不再用 dict）；high_score 占位传 int 0（与 renderer 类型一致），HUD 第 1 行显示 "High: 0" | §3.4 / §4.6 |
+| R2-4 (P0-4) | 应实现 | GameState 构造：`GameState(width=20, height=15, difficulty=..., rng=Random())` **全关键字参数**；删除位置参数所有引用 | §3.4 / §4.4 / §6.4 |
+| R2-5 (P0-5) | 应实现 | MENU / GAME_OVER 渲染方案 = **game-app 自绘**（直接 `pygame.font` 渲染文字 + `pygame.draw` 画矩形/分隔线）；主循环在 MENU / GAME_OVER 态**不调 `renderer.render`**（renderer 只画游戏画面），game_state=None 时不构造 snapshot | §4.6 / §4.8 / §5.3 |
+| R2-6 (P0-6) | 应实现 | 依赖版本声明明确"以 game-core 迭代 2 接口为准"（dev_reviewing 中，落定后开发）；若 iter-2 检视 FAIL 改回 iter-1 形态则依赖模块契约重核 | §附录 B |
+| R2-7 (P1-1) | 应实现 | 难度选择单一模型：**保留 1/2/3 快捷键 + Enter/START 启动**；删除 `MenuCursor` 死代码、`SELECT_*` 与 `MOVE_*` 混用 | §1.2 / §4.4 |
+| R2-8 (P1-2) | 应实现 | QUIT 单一通道：主循环只检测 actions 含 QUIT 即 break；**删除** `_quit_requested` 标志位 | §4.2 / §4.4 |
+| R2-9 (P1-3) | 应实现 | "任意键开始"：MENU 态下 `_map_event` 把所有**未映射**的 KEYDOWN 归一为 `InputAction.START` | §4.3 |
+| R2-10 (P1-4) | 应实现 | 窗口尺寸契约：`AppConfig.window_w/window_h` **= (640, 480)**（与 gui-renderer `WINDOW_WIDTH/HEIGHT` 常量一致）；最小可玩尺寸由 renderer 校验，app 不预校验 | §3.1 / §3.4 |
+| R2-11 (P2-1) | 文档 | `_pause_hint_shown` 在 §1.3 运行期状态表声明 | §1.3 |
+| R2-12 (P2-2) | 文档 | 删除占位又注释掉的 `PAUSED` 枚举；保持 `AppScreen` 仅含 MENU/PLAYING/GAME_OVER 三态 | §1.1 / §3.2 |
+| R2-13 (P2-3) | 实现 | `difficulty_label` 直接传中文显示："简单"/"普通"/"困难"（按规格 Q-01 拍板命名） | §4.6 / §6.4 |
+| R2-14 (P2-4) | 文档 | 难度选择入口提前到迭代 1 的说明：R-01 用户拍板纳入本轮，与架构"迭代 2 难度选择界面"范围重复；本设计明示**迭代 2 仅承接最高分展示与重置、不重复难度 UI** | §附录 B / 迭代 2 预告 |
+| R2-15 (P2-5) | 实现 | 文档 / UT 中不引用 `on_score` 事件名；迭代 2 实装 `set_score_callback(cb)` 时按 iter-2 落地形态对接 | §附录 A |
+| R2-16 (P2-6) | 实现 | conftest 桩补 `pygame.event.get()` 事件队列注入，让端到端 UT 用例 28 可跑 | §6.2 |
+| R2-17 (P2-7) | 文档 | INV-3 描述由"`set_difficulty` raise NotImplementedError"改为"难度固化于 GameState 字段，无运行中修改接口"（与 game-core 实际接口一致） | §1.4 |
+
+> **未变更（沿用 r1）**：模块定位 / 不引入 / Python 3.8 兼容 / 零配置 / 无网络 / 无音效 / 不可变函数语义 / 不写文件 / 退出码 0/1/2 / 单一职责与文件组织 / 主事件循环结构 / 不导入 platform_storage。
+
+---
+
+## 0. 模块定位与迭代边界
+
+| 项 | 值 |
+|----|---|
+| 模块 | game-app |
+| 类型 | 上层应用 |
+| 依赖 | game-core（纯逻辑，迭代 2 dev_reviewing — 接口以 iter-2 落地为准）、gui-renderer（迭代 1 it_passed — 契约已锁定）、platform-storage（迭代 2 接入） |
+| 被依赖 | 无（顶层装配） |
+| 承载需求 | snake-gui **主体**（FR-01~16 中除 gui-renderer 子集外的全部）—— 本迭代 1 范围 = FR-01~04 玩法闭环入口 + FR-05 难度选择入口 + FR-11 开始/结束/重开/退出 |
+| 迭代 | 1（首发） |
+| 不引入 | 第三方除 pygame 外任何依赖；不引入音效（架构 §R-04）；不引入网络（NFR-06）；不引入 config 文件（架构 §配置模型）；不写系统目录（便携式，NFR-07） |
+| 跨迭代复用 | 主循环骨架 / 界面状态机 / 输入映射 / 错误处理框架 跨 4 迭代复用；迭代 2/3 通过**新增状态节点与处理函数**接入，不重写主循环 |
+| PyInstaller 入口 | `snake-gui.py`（包根 `__main__.py`，`if __name__ == "__main__": main()`） |
+
+### 迭代 1 出口（与架构 §迭代计划对齐；难度选择入口按 R-01 提前到迭代 1，见 R2-14）
+
+- ✅ 主事件循环（pygame 帧驱动，clock.tick 60FPS）
+- ✅ 输入映射（WASD/方向键 → core.set_direction；P 键迭代 1 不调 `toggle_pause`，仅记 hint 显示；Q/ESC 退出）
+- ✅ 界面状态机：`MENU` → `PLAYING` → `GAME_OVER` →（重开 → `PLAYING` / 退出）
+- ✅ 开始界面：**app 自绘**（标题 + "按任意键开始" + 难度选择 1/2/3 三档快捷键 + Enter 确认）
+- ✅ 游戏 HUD：`HudData` dataclass（5 字段；high_score=0 占位，迭代 2 接入持久化）
+- ✅ 结束画面：**app 自绘**（"Game Over" + 最终得分 + "R 重开 / Q 退出"）
+- ✅ 退出 1 秒内无残留进程（FR-11，`Renderer.shutdown()` 兜底）
+- ✅ 帧率 ≥ 60 FPS（pygame clock 兜底 + `Renderer.fps_metric()` 回归）
+- ❌ 暂停/继续（FR-12，迭代 2）
+- ❌ 最高分持久化（FR-13，迭代 2；`set_score_callback` 入口已在 iter-2 core 暴露）
+- ❌ 皮肤切换 UI（FR-10，迭代 3；当前仅经典皮肤）
+- ❌ 窗口缩放（FR-09，迭代 3）
+- ❌ 平滑动画（FR-07，迭代 3）
+- ❌ 三平台打包（FR-14/15，迭代 4）
+- ❌ 完善错误提示（NFR-03 最小集仅图形环境不可用 → 退出码 2；迭代 4 完善）
+
+### 迭代 1 已知技术约束（FO 实现必读）
+
+1. **Python 3.8 兼容**：与架构 §代码风格约定一致，不使用 dataclass 自定义 `__setattr__` 之外的 3.9+ 特性；`Optional[X]` / `Tuple[X, ...]` 而非 `X | None` / `tuple[X, ...]`。
+2. **零配置**：不读 ini/env/YAML/JSON 配置；难度通过游戏内 UI 选择。
+3. **无网络**：全模块不 `import socket` / `import urllib` / `import http` / `import requests`；UT 不发起网络。
+4. **无音效**：不 `import pygame.mixer` 或任何音频模块。
+5. **依赖边界**：game-app **可** import pygame（事件循环 + 字体 + 自绘菜单/结束画面）；**不可**侵入 game-core 内部（仅通过 game-core 的公开 API：`Point`/`Direction`/`Difficulty`/`GameStatus`/`GameState`/`Snapshot`/`InvalidStateError`/`set_score_callback`）；**不可**直接改 gui-renderer 内部状态（仅通过 `Renderer(window_size, skin=DEFAULT_SKIN)` 构造、`init()` / `shutdown()` 生命周期、`render(snapshot, hud)` 帧绘制、`skin` property 只读）。
+6. **platform-storage 迭代 1 不导入**：`import platform_storage` 不出现在迭代 1 代码中；high_score 占位为 0。
+
+---
… omitted 888 diff line(s) across 1 additional file(s)/section(s)

session_id: 20260814_030003_8568e4
已完成，状态从 `design_working` → `design_reviewing`，审计行已写入 pipeline.log。

修订版 `snake-linux/design/game-app/设计-r2.md` 已落盘，r1 已归档至 `archive/design-r1/snake-linux-game-app-design-r1.md`。
  ┊ review diff
a/workspace/snake-linux/design/game-app/设计-r3.md → b/workspace/snake-linux/design/game-app/设计-r3.md
@@ -0,0 +1,1114 @@
+# 功能模块设计：game-app（snake-linux v2.0.0 迭代 1）r3（修订版，回应 SE 评审 r2 FAIL）
+
+> MDE r3 · 修订版 · 严格对齐架构 `snake-linux/arch/v2.0.0/架构设计.md`、分工表 `snake-linux/arch/v2.0.0/功能模块分工表.md`、需求规格 `snake-linux/analysis/snake-gui-r1.md`（R-01~R-09 已固化）
+> 依赖模块实际契约：game-core **迭代 2**（`code/game-core/iter-2/game_core/` — **it_passed，契约已锁定**）、gui-renderer **迭代 1**（`code/gui-renderer/iter-1/gui_renderer/` — it_passed，**契约已锁定**）、platform-storage（迭代 2 接入，本迭代不调用）
+> 评审历史：r1 `snake-linux-game-app-design-iter1-r1.md`（FAIL，6×P0） → r2 `snake-linux-game-app-design-iter1-r2.md`（FAIL，r1 全修订后**新增 1×P0 + 1×P1**） → **本版 r3**
+> 修订要点：见下表 + §附录 D 修订对照
+> **目标**：FO 拿到本文即可 TDD 开发；r2 评审 P0/P1 已全部给出对齐锁定代码 + 一致表述的写法；P2 列表 13 项一并同步
+
+---
+
+## 修订摘要（响应 SE 评审 r2 FAIL）
+
+### r2 评审的 1×P0 + 1×P1（必须修订）
+
+| ID | 级别 | 修订内容 | 章节 |
+|----|------|----------|------|
+| **R3-1 (P0-1)** | 应实现 | **None→START 转换点统一为 `_drain_events`**（屏态兜底）；MENU 态**除 QUIT/SELECT_*/TOGGLE_PAUSE/RESTART 外**所有 KEYDOWN（含未映射键、方向键、WASD、字母数字）均归一为 `InputAction.START`；`_dispatch_menu` 删 None 分支；§4.3 注 / §4.4 注释 / 修订摘要 / §4.4 代码四处表述统一；菜单提示同步改"Enter / 空格 / 其他键 开始" | §3.3 §4.3 §4.4 §4.9 §6.4 UT-9/38 |
+| **R3-2 (P1-1)** | 应实现 | menu.py 自绘改用 **`pygame.display.get_surface()`**（公开 API，等价 `_screen`，不碰 renderer 私有属性）；自绘函数形参从 `App` 收窄为 `(surface, title_font, body_font, difficulty) -> None` 与 `(surface, title_font, body_font, score) -> None`；`§0.5 边界` 与 `§4.10.4 不读 renderer 私有` 维持原约束，与本文实现一致 | §4.7 §4.8 §5.4 §6.2 |
+
+### P2 列表 13 项（顺手同步修订）
+
+| ID | 级别 | 修订内容 | 章节 |
+|----|------|----------|------|
+| R3-3 (P2-1) | 文档 | 分工表迭代 2 出口备注"难度 UI 已在迭代 1 完成，迭代 2 仅承接最高分展示+重置"；分工具/架构迭代计划补同步一行 | §附录 B |
+| R3-4 (P2-2) | 文档 | 字段命名统一：`_selected_difficulty` → `_difficulty`、`difficulty` → `_difficulty`、`high_score` → `_high_score`；全文 + conftest + UT 同步 | §1.2 §1.3 §4.4 §4.6 §6.2 §6.4 |
+| R3-5 (P2-3) | 文档 | §1.3 运行期状态表补 `_running: bool = True`（主循环退出标志，由 QUIT 通道置 False） | §1.3 §3.4 §4.4 |
+| R3-6 (P2-4) | 文档 | game-core 迭代 2 状态由 "dev_reviewing" 改为 **"it_passed，契约已锁定"**；R2-6 的 iter-2 fallback 流程降级为"若未来 core 改动需重核"（不再有 FAIL 改回风险） | §0 §附录 B |
+| R3-7 (P2-5) | 实现 | **删除 `_quit()` 死代码与 `_dispatch_menu`/`_dispatch_over` 的 QUIT 分支**（主循环 `if QUIT in actions: break` 先行退出，分支永远不可达）；dispatch 内需要"退出"语义时统一写 `self._running = False`（FOR 未来 OVER→MENU 等场景） | §3.4 §4.2 §4.4 |
+| R3-8 (P2-6) | 实现 | `_tick` **循环内重读 tick_ms**（去掉帧首快照漂移）：`while self._tick_accumulator_ms >= tick_ms: tick_ms = self.game_state.snapshot().tick_ms; ...`，逐拍取值对齐 INV-4 语义"调后减 tick_ms" | §4.5 §6.4 UT-19/20 |
+| R3-9 (P2-7) | 文档 | §5.6 错误矩阵"InvalidStateError → AppError 子类包装"改为 **"理论不可达（INV-1/2 保护），不包装；UT 覆盖不触发**"；`_dispatch_playing` 移除隐含的 try/except | §4.4 §5.6 |
+| R3-10 (P2-8) | 实现 | `App.__init__` **只置 `self._renderer = None`**；`Renderer(...)` 构造与 `init()` 都在 `_init_pygame`，UT-1 断言改为"构造后 `_renderer is None`" | §3.4 §4.7 §6.4 UT-1 |
+| R3-11 (P2-9) | 实现 | 删 `event_queue` 占位 fixture；§6.2 conftest 用 `fake_pygame.event.get.return_value` / `side_effect=[[evt1],[evt2],...]` 注入；§5.4 自绘用例统一在 `app_in_playing` fixture 内 `_init_pygame()` 后用 | §5.4 §6.2 §6.4 UT-28/30/35 |
+| R3-12 (P2-10) | 实现 | **CJK 字体回退链**：`pygame.font.match_font` 候选 `["notosanscjksc", "notosanscjk", "wenquanyizenhei", "wenquanyimicrohei", "arialunicodems"]`，全失败 → `pygame.font.Font(None, size)` 兜底；`_init_pygame` 用 `match_font` 一次构造 `_menu_title_font` / `_menu_body_font` / `_hud_font`（CJK 字体同时给 renderer HUD 共享） | §4.7 §5.4 |
+| R3-13 (P2-11) | 实现 | `_render` PLAYING 路径**只取一次 snap**：`snap = self.game_state.snapshot(); hud = self._build_hud(snap); self._renderer.render(snap, hud)`；`_build_hud` 签名加 `snap` 参数 | §4.6 §4.8 §6.4 |
+| R3-14 (P2-12) | 文档 | conftest 提供 `app_in_playing` fixture（已 `_init_pygame` + 进入 PLAYING 态）；自绘用例 UT-28/30 改用 `app_in_playing` 触发；`app` fixture 维持"未 init、未开局" | §6.2 §6.4 |
+| R3-15 (P2-13) | 实现 | 退出码 2 失败路径也兜底：`run()` 进入 `try: self._init_pygame() except GraphicsUnavailableError as e: ... finally: try self._renderer.shutdown() except: pass`（即使 init 抛错，shutdown 也尝试一次，幂等安全） | §4.2 §6.4 UT-32 |
+
+### 未变更（沿用 r2）
+
+模块定位 / 不引入 / Python 3.8 兼容 / 零配置 / 无网络 / 无音效 / 不可变函数语义 / 不写文件 / 退出码 0/1/2 / 单一职责与文件组织 / 主事件循环结构 / 不导入 platform_storage / r1 的 6×P0 全保留 / r2 的 11×P2（除上面 P2 同步项）。
+
+---
+
+## 0. 模块定位与迭代边界
+
+| 项 | 值 |
+|----|---|
+| 模块 | game-app |
+| 类型 | 上层应用 |
+| 依赖 | game-core（纯逻辑，迭代 2 **it_passed — 接口以 iter-2 落地为准**，R3-6）、gui-renderer（迭代 1 it_passed — 契约已锁定）、platform-storage（迭代 2 接入） |
+| 被依赖 | 无（顶层装配） |
+| 承载需求 | snake-gui **主体**（FR-01~16 中除 gui-renderer 子集外的全部）—— 本迭代 1 范围 = FR-01~04 玩法闭环入口 + FR-05 难度选择入口 + FR-11 开始/结束/重开/退出 |
+| 迭代 | 1（首发） |
+| 不引入 | 第三方除 pygame 外任何依赖；不引入音效（架构 §R-04）；不引入网络（NFR-06）；不引入 config 文件（架构 §配置模型）；不写系统目录（便携式，NFR-07） |
+| 跨迭代复用 | 主循环骨架 / 界面状态机 / 输入映射 / 错误处理框架 跨 4 迭代复用；迭代 2/3 通过**新增状态节点与处理函数**接入，不重写主循环 |
+| PyInstaller 入口 | `snake-gui.py`（包根 `__main__.py`，`if __name__ == "__main__": main()`） |
+
+### 迭代 1 出口（与架构 §迭代计划对齐；难度选择入口按 R-01 提前到迭代 1，见 R2-14 / R3-3）
+
+- ✅ 主事件循环（pygame 帧驱动，clock.tick 60FPS）
+- ✅ 输入映射（WASD/方向键 → core.set_direction；P 键迭代 1 不调 `toggle_pause`，仅记 hint 显示；Q/ESC 退出）
+- ✅ 界面状态机：`MENU` → `PLAYING` → `GAME_OVER` →（重开 → `PLAYING` / 退出）
+- ✅ 开始界面：**app 自绘**（标题 + "Enter / 空格 / 其他键 开始" + 难度选择 1/2/3 三档快捷键 + Enter 确认，R3-1 任意键语义统一）
+- ✅ 游戏 HUD：`HudData` dataclass（5 字段；high_score=0 占位，迭代 2 接入持久化）
+- ✅ 结束画面：**app 自绘**（"Game Over" + 最终得分 + "R 重开 / Q 退出"）
+- ✅ 退出 1 秒内无残留进程（FR-11，`Renderer.shutdown()` 兜底）
+- ✅ 帧率 ≥ 60 FPS（pygame clock 兜底 + `Renderer.fps_metric()` 回归）
+- ❌ 暂停/继续（FR-12，迭代 2）
+- ❌ 最高分持久化（FR-13，迭代 2；`set_score_callback` 入口已在 iter-2 core 暴露）
+- ❌ 皮肤切换 UI（FR-10，迭代 3；当前仅经典皮肤）
+- ❌ 窗口缩放（FR-09，迭代 3）
+- ❌ 平滑动画（FR-07，迭代 3）
+- ❌ 三平台打包（FR-14/15，迭代 4）
+- ❌ 完善错误提示（NFR-03 最小集仅图形环境不可用 → 退出码 2；迭代 4 完善）
+
+### 迭代 1 已知技术约束（FO 实现必读）
+
+1. **Python 3.8 兼容**：与架构 §代码风格约定一致，不使用 dataclass 自定义 `__setattr__` 之外的 3.9+ 特性；`Optional[X]` / `Tuple[X, ...]` 而非 `X | None` / `tuple[X, ...]`。
… omitted 1036 diff line(s) across 1 additional file(s)/section(s)

session_id: 20260814_032013_cbb69b
状态正确：`design_reviewing`，审计行 `MODULE_DESIGN snake-linux/game-app iter-1 DONE -> design_reviewing`，spawn SE 评审会被下次 tick 接管。任务完成，无需汇报。
