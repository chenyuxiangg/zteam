# 功能模块设计：game-core（snake-linux v2.0.0 迭代 1）

> MDE 产出 · 对齐架构 `snake-linux/arch/v2.0.0/架构设计.md` 与分工表 `功能模块分工表.md`
> 迭代 1 范围：玩法闭环（移动/吃食/碰撞/反向禁止/得分/状态机 run+over）+ 难度参数表（EASY/MEDIUM/HARD 一次到位，**无加速曲线**——加速曲线放迭代 2 与暂停一同落地）
> 迭代 2 增量：暂停状态机、speed_curve(score)、on_score 事件
> **零 GUI 依赖、纯标准库、可独立 UT**（NFR-05）

---

## 数据结构

### 1.1 基础值对象

| 类型 | 字段 | 说明 |
|------|------|------|
| `Point`（值对象） | `x: int`、`y: int` | 网格坐标。y 向下、0,0 为左上角，沿用 v1 约定 |
| `Direction`（枚举） | `UP=(0,-1)` / `DOWN=(0,1)` / `LEFT=(-1,0)` / `RIGHT=(1,0)` | 4 向；`opposite()` 工具方法返回反向 |
| `GameStatus`（枚举） | `RUN` / `PAUSED` / `OVER` | 运行态机；迭代 1 仅 RUN/OVER，PAUSED 枚举先占位但不允许进入 |
| `Difficulty`（枚举） | `EASY` / `MEDIUM` / `HARD` | 难度档位，绑定 `base_tick_ms` |

### 1.2 难度参数表

```python
DIFFICULTY_PARAMS = {
    Difficulty.EASY:   {"base_tick_ms": 250},
    Difficulty.MEDIUM: {"base_tick_ms": 160},
    Difficulty.HARD:   {"base_tick_ms": 100},
}
```

> 迭代 1 仅落 base_tick_ms。加速曲线（`speed_curve(score) -> tick_ms`）放迭代 2；迭代 1 内部不出现 score→tick 的换算逻辑，UI 层直接用 `base_tick_ms`。

### 1.3 核心实体

**`Snake`**
- 字段：`body: tuple[Point, ...]`（**不可变 tuple**，蛇头 = `body[0]`，蛇尾 = `body[-1]`）
- 不变量：`len(body) >= 1`；相邻两节点必须 4-邻接
- 派生：`head -> Point`、`len -> int`
- 关键方法（生成新对象，不修改自身）：
  - `with_head(new_head) -> Snake`：在头部插入新节点（移动未吃食）
  - `without_tail() -> Snake`：去掉尾部（移动未吃食，与 `with_head` 配对）
  - `with_head_no_tail_drop(new_head) -> Snake`：仅头部插入（吃食后不丢尾）
  - `contains(p) -> bool`：判断点是否在蛇身

**`Food`**
- 字段：`pos: Point`
- 不变量：`pos not in snake.body`

**`GameState`**
- 字段：
  - `width: int`（网格宽，列数；建议默认 20）
  - `height: int`（网格高，行数；建议默认 15）
  - `difficulty: Difficulty`
  - `snake: Snake`（初始 3 节，居中朝右：body=[(W//2, H//2), (W//2-1, H//2), (W//2-2, H//2)]）
  - `direction: Direction`（初始 RIGHT）
  - `pending_direction: Direction | None`（**同帧多输入合并用**：每个 step 取一次最终方向，规避一帧内连按 WASD 跳过反向的漏洞）
  - `food: Food`
  - `score: int`（**每节长度+1 = 1 分**；迭代 1 不引入复杂计分）
  - `status: GameStatus`
  - `rng: random.Random`（注入，便于 UT 确定性，**禁止隐式全局 random**）
  - `_last_step: int | None`（节拍序号，用于事件订阅；迭代 1 可不暴露）

> 迭代 1 不引入 `events` 队列、`on_score` 回调（事件机制放迭代 2 与 `platform-storage` 接入时一起做）；得分判定由调用方读 `state.score` 即可。

### 1.4 不变量清单（FO 实现必须保证，UT 也要覆盖）

| ID | 不变量 | 违反后果 |
|----|--------|----------|
| INV-1 | `len(snake.body) >= 1` 且相邻节点 4-邻接 | 移动逻辑破坏 |
| INV-2 | `food.pos not in snake.body` | 同位 bug |
| INV-3 | `0 <= head.x < width` 且 `0 <= head.y < height` | 出界 bug（撞墙判定在 step 入口先做） |
| INV-4 | `status == OVER` 后所有 step/set_direction 不再改变 snake/food/score | 终态保护 |
| INV-5 | `pending_direction` 若非 None，则与 `direction` 非 opposite（受理时校验） | 反向移动禁止 |
| INV-6 | `rng` 始终是注入实例，模块内不出现 `import random` 后直接 `random.xxx` | 确定性测试前提 |

---

## 数据传递方式

| 通道 | 形态 | 用途 |
|------|------|------|
| **构造注入** | `GameState(width, height, difficulty, *, rng=None, initial_direction=RIGHT)` | 注入 RNG 与初始方向；UT 用 `Random(seed)` 复现 |
| **参数传入** | `set_direction(d: Direction)` / `step() -> GameState` | 不在类内维护隐式时间轴；调用方按节拍显式 step |
| **不可变快照** | `snapshot() -> Snapshot`（dataclass 或 typing.NamedTuple，**冻结**） | 供 gui-renderer 读、供 app 判定终态；禁止 renderer 改 state |
| **返回值** | `step()` **返回新 GameState**（不原地修改） | 纯函数语义，UT 断言前后状态对比；与 v1 就地修改的 API 不同，**这是 v2 设计改进** |
| **异常** | `DirectionError`、`InvalidStateError`（继承 `ValueError`） | 错误输入显式失败，不静默吞 |

> **关键决策（与 v1 的差异）**：`step()`/`set_direction()` 不修改入参对象，而是返回新 `GameState`。这让"上帧状态"在 renderer / UT 中天然可保留，回放/动画/对比都简单。FO 实现时须在 docstring 强调此点。

---

## 对外接口

### 2.1 `Difficulty` 枚举
```python
class Difficulty(Enum):
    EASY = "easy"
    MEDIUM = "medium"
    HARD = "hard"

    @property
    def base_tick_ms(self) -> int: ...   # 250 / 160 / 100
```

### 2.2 `Point`
```python
@dataclass(frozen=True)
class Point:
    x: int
    y: int
```

### 2.3 `Direction` 枚举
```python
class Direction(Enum):
    UP    = (0, -1)
    DOWN  = (0,  1)
    LEFT  = (-1, 0)
    RIGHT = (1,  0)

    @property
    def dx(self) -> int: ...
    @property
    def dy(self) -> int: ...
    def opposite(self) -> "Direction": ...  # UP<->DOWN, LEFT<->RIGHT
```

### 2.4 `GameState`
```python
class GameState:
    def __init__(
        self,
        width: int,
        height: int,
        difficulty: Difficulty,
        *,
        rng: random.Random | None = None,
        initial_direction: Direction = Direction.RIGHT,
    ): ...

    # --- 只读属性 ---
    @property
    def head(self) -> Point: ...
    @property
    def score(self) -> int: ...
    @property
    def status(self) -> GameStatus: ...
    @property
    def snake(self) -> Snake: ...
    @property
    def food(self) -> Food: ...
    @property
    def direction(self) -> Direction: ...

    # --- 命令式接口（返回新 GameState，不修改 self）---
    def set_direction(self, d: Direction) -> "GameState":
        """登记期望方向；同一节拍内多次调用以最后一次为准。
        规则：若 d 与当前 direction 反向，忽略（不抛错，便于游戏内连续按 WS 被吃）。
        规则：当 status==OVER，抛 InvalidStateError。
        规则：若 d 与当前 direction 相同，直接返回 self（幂等）。"""

    def step(self) -> "GameState":
        """推进一个节拍：
          1. 校验 status==RUN，否则抛 InvalidStateError
          2. 计算 next_head = head + d(direction)
          3. 撞墙：next_head 越界 → status=OVER，蛇身/食物/得分不变，返回新 state
          4. 撞自身：next_head in snake.body → status=OVER，同上
          5. 吃食：next_head == food.pos → score += 1，生成新 food（排除蛇身），蛇身 = [next_head, *body]
          6. 普通移动：蛇身 = [next_head, *body[:-1]]
          7. 提交 pending_direction → direction（每次 step 一次性消费）
        返回新 GameState。"""

    def snapshot(self) -> "Snapshot":
        """返回不可变快照：
          snake_body: tuple[Point, ...]
          food: Point
          score: int
          length: int
          status: GameStatus
          difficulty: Difficulty
          tick_ms: int   # 当前应使用的节拍毫秒数（迭代 1 = difficulty.base_tick_ms）
        """
```

### 2.5 `Snapshot`（值对象）
```python
@dataclass(frozen=True)
class Snapshot:
    snake_body: tuple[Point, ...]
    food: Point
    score: int
    length: int
    status: GameStatus
    difficulty: Difficulty
    tick_ms: int
```

### 2.6 异常
```python
class DirectionError(ValueError): ...   # 当前迭代不抛（反向移动静默忽略），保留为未来扩展
class InvalidStateError(RuntimeError):
    """对 OVER/PAUSED 状态调用 set_direction/step/toggle_pause 时抛出"""
```

### 2.7 公开 API 列表（供 FO 实现核对）

| 名称 | 类型 | 用途 |
|------|------|------|
| `Point` | dataclass | 坐标值对象 |
| `Direction` | Enum | 4 向 + opposite/dx/dy |
| `Difficulty` | Enum | 难度 + base_tick_ms |
| `GameStatus` | Enum | RUN / PAUSED / OVER |
| `GameState` | class | 主控类 |
| `Snapshot` | dataclass(frozen) | 不可变状态快照 |
| `InvalidStateError` | Exception | 非法状态转换 |
| `DIFFICULTY_PARAMS` | dict（仅迭代 1 用） | 难度→base_tick_ms 映射；迭代 2 替换为 speed_curve |

---

## 实现细节/步骤

### 3.1 模块文件组织
```
game_core/
├── __init__.py          # 对外 re-export：Point, Direction, Difficulty, GameStatus, GameState, Snapshot, InvalidStateError
├── types.py             # Point, Direction, Difficulty, GameStatus, Snapshot
├── params.py            # DIFFICULTY_PARAMS（迭代 2 改为 speed_curve）
├── state.py             # GameState（含 step/set_direction/snapshot）
└── errors.py            # InvalidStateError, DirectionError
```

### 3.2 核心流程（step 内部）

```
set_direction(d):
  if status==OVER: raise InvalidStateError
  if d == direction: return self            # 幂等
  if d == direction.opposite(): return self  # 反向忽略（静默，不抛错）
  return self.copy(pending_direction=d)

step():
  if status != RUN: raise InvalidStateError
  d = pending_direction or direction
  next_head = Point(head.x + d.dx, head.y + d.dy)
  new_status = RUN

  # 撞墙
  if not (0 <= next_head.x < width and 0 <= next_head.y < height):
      new_status = OVER
      return self.copy(status=OVER, pending_direction=None)

  # 撞自身（注意：蛇尾在下一步会移除，所以"撞移动前的蛇尾"不算撞，蛇身判断应基于移动后蛇身
  #   即不包含旧尾；为简化，迭代 1 判定时不排除旧尾——只有当 new_head == 旧尾时才允许，
  #   其余身段均视为撞。实现：if new_head in body[1:] OR new_head == body[-1]（尾特例））
  # 简化规则（FO 必须按此实现）：撞自身判定 = new_head in set(body)
  #   含义：撞尾也算结束（v1 行为保持一致）。v2 不引入"追尾不算"的特殊规则。
  if next_head in set(snake.body):
      new_status = OVER
      return self.copy(status=OVER, pending_direction=None)

  # 吃食
  if next_head == food.pos:
      new_snake = snake.with_head_no_tail_drop(next_head)
      new_food = spawn_food(rng, width, height, new_snake)  # 排除蛇身
      new_score = score + 1
  else:
      new_snake = snake.with_head(next_head).without_tail()
      new_food = food
      new_score = score

  return self.copy(
      snake=new_snake, food=new_food, score=new_score,
      direction=d, pending_direction=None, status=new_status,
  )
```

### 3.3 食物生成（`spawn_food`）

```
1. 收集所有空闲格 = {(x,y) for x in [0,W) for y in [0,H)} - set(snake.body)
2. 若空闲格为空（蛇填满全屏）→ 抛 RuntimeError("No space for food")  # 实际几乎不可能，但鲁棒性要兜
3. choice(rng, list(空闲格)) → 新食物点
```

> **性能注**：网格默认 20×15=300 格，set 差集 + 随机选对单步开销可忽略（<0.1ms）。不引入空间索引。

### 3.4 初始布局

- 蛇：3 节，居中朝右，body=[(W//2, H//2), (W//2-1, H//2), (W//2-2, H//2)]
- 食物：随机一个非蛇身位置
- direction = RIGHT
- score = 0
- status = RUN

### 3.5 状态机（迭代 1 子集）

```
       step (撞墙/撞自身)        step (正常/吃食)
NEW ───────────────────▶ OVER      RUN ────────────▶ RUN
       set_direction(OVER)→InvalidStateError
       set_direction(RUN)→生效
       step(OVER)→InvalidStateError
```

迭代 1 **不实现** PAUSED 转换；`toggle_pause` 不在本迭代接口列表中。GameStatus.PAUSED 枚举占位但不暴露入口。

### 3.6 与 v1 差异（FO 实现须知）

| 维度 | v1（snake-linux v1.0.0） | v2（本次） |
|------|--------------------------|------------|
| step 语义 | 原地修改 | 返回新对象 |
| 蛇身结构 | list | tuple（不可变） |
| RNG | 全局 random | 注入 |
| 状态机 | run/over | run/paused/over（paused 迭代 2） |
| 加速曲线 | 无 | 迭代 2 落 |
| 反向移动 | 禁止 | 静默忽略（不抛错） |

### 3.7 实现注意点

1. **纯函数化**：`step`/`set_direction` 必须返回新对象；用 `dataclasses.replace` 或自定义 `copy()` 简化。
2. **不可变 snake.body**：所有"移动/吃食"操作走 `with_head*` / `without_tail`；避免 list 原地 append。
3. **RNG 注入**：`__init__` 默认 `rng=random.Random()`（**非全局 random**），UT 显式传 `Random(42)`。
4. **不做计时**：core 不知道"时间"，仅按"节拍"推进。`tick_ms` 由 `Difficulty.base_tick_ms` 提供，渲染层/主循环按此定时。
5. **不做渲染**：core 不输出任何字符/颜色/图像，只产出 snapshot。
6. **不读盘/不写盘**：core 不引入任何 I/O，不读最高分，不写日志。
7. **import 约束**：本模块**仅可 import 标准库**（`enum`、`dataclasses`、`random`、`typing`），禁止 `import pygame` 等。

---

## DFx / 可测试性 / 鲁棒性 / 韧性

### 4.1 可维护性（Maintainability）
- 每个公开类/方法有 docstring，标注对应 FR/NFR 编号（如 `"""FR-01~05 玩法核心。NFR-05 零 GUI 依赖。"""`）
- 关键不变量在代码中以注释 + UT 用例双标注
- 单一职责：state.py 只管状态推进；types.py 只管值对象；params.py 只管参数表

### 4.2 可扩展性（Extensibility）
- 难度参数集中在 `params.py`，迭代 2 加 `speed_curve(score)` 时只改这一处
- `GameStatus.PAUSED` 枚举已占位，迭代 2 加 `toggle_pause()` 不破坏 API
- 食物生成抽象为内部函数，迭代 3 若需"特殊食物"可扩展

### 4.3 可部署性（Deployability）
- 纯标准库，PyInstaller 打包时**无需 hook**
- 单一包目录 `game_core/`，可独立 wheel 化
- 无 C 扩展、无平台特定代码

### 4.4 可测试性（Testability）
- **零 GUI 依赖**：UT 全部在内存里跑，无需显示器/窗口
- **RNG 注入**：UT 可固定 seed 复现
- **纯函数化**：`step` 返回新对象，UT 可断言 `old == snapshot_before` 不被改
- **可枚举**：`width × height ≤ 300`（默认网格），暴力枚举所有位置组合做穷举 UT 可行
- **无时间/IO**：不需要 mock time.sleep、不需要 tmp_path

### 4.5 鲁棒性 / 韧性
| 场景 | 处理 |
|------|------|
| 反向输入 | 静默忽略（连续按 WS 不致死） |
| 同一节拍多次 set_direction | 取最后一次（pending_direction） |
| status==OVER 后继续 step | 抛 `InvalidStateError` |
| 状态推进中食物生成冲突 | 排除蛇身后随机；全屏填满抛 RuntimeError |
| 网格宽高 < 3 或 0 | `__init__` 抛 `ValueError`（最小能放 3 节蛇） |
| 难度传入非法值 | `Difficulty` 枚举约束，构造时类型即拦截 |
| 初始 RNG 不传 | 默认 `random.Random()`（**实例，非全局**），UT 显式传入以保证可复现 |

### 4.6 错误处理矩阵

| 错误情形 | 行为 |
|----------|------|
| `set_direction` 时 status==OVER | `InvalidStateError` |
| `set_direction` 反向 | 静默忽略，返回 self |
| `step` 时 status==OVER/PAUSED | `InvalidStateError` |
| `__init__` width<3 or height<3 | `ValueError` |
| `__init__` difficulty 非 Difficulty 枚举 | `TypeError`（枚举自动） |
| 食物生成时空闲格为 0 | `RuntimeError("No space for food")`（极端情况，理论上 20×15 不可能触发） |

---

## 资源评估

| 资源 | 评估 |
|------|------|
| **CPU** | `step` 单次 O(W·H) 用于食物生成冲突检测（set 差集），默认 300 格 <0.1ms；移动 O(1)（不含尾部）。整体 CPU 占用 <1% 单核 |
| **内存** | `GameState` 单对象 <1KB；`Snapshot` 拷贝蛇身 tuple（W·H ≤ 300 个 Point ≈ 5KB）。千帧/秒不积压 |
| **存储** | 0（无 I/O） |
| **外部依赖** | 0（仅 Python 3.8+ 标准库） |
| **线程** | 非线程安全；调用方（app 主循环）单线程访问，UT 单线程跑 |
| **GIL** | 不影响（无阻塞 I/O、无 C 扩展） |
| **打包体积** | 0 增量（PyInstaller 不会把 core 单独算） |

---

## UT 框架（FO TDD 依据）

### 5.1 测试组织

```
tests/
├── test_game_core/
│   ├── __init__.py
│   ├── conftest.py                  # 共享 fixture：固定 RNG、固定难度、默认网格
│   ├── test_point.py                # Point 值对象：相等性、哈希、可哈希
│   ├── test_direction.py            # Direction 枚举：dx/dy/opposite 配对正确
│   ├── test_difficulty.py           # Difficulty.base_tick_ms 表值
│   ├── test_state_init.py           # 构造：初始蛇位置、初始方向、初始食物不在蛇身、score=0
│   ├── test_state_set_direction.py  # set_direction 各种输入
│   ├── test_state_step_move.py      # 普通移动：蛇头前进、蛇尾移除
│   ├── test_state_step_eat.py       # 吃食：score+1、不丢尾、新食物不在蛇身
│   ├── test_state_step_collide_wall.py  # 撞墙 → OVER
│   ├── test_state_step_collide_self.py  # 撞自身（含撞尾）→ OVER
│   ├── test_state_step_over_guard.py    # OVER 后 step 抛 InvalidStateError
│   ├── test_state_direction_pending.py  # 同节拍多次 set_direction 取最后一次
│   ├── test_state_reversal_block.py     # 反向输入被静默忽略
│   ├── test_state_snapshot.py       # snapshot 不可变、字段一致
│   ├── test_state_determinism.py    # 固定 seed → 固定初始食物
│   └── test_state_food_spawn.py     # 食物生成排除蛇身；饥饿极限（全屏=蛇）抛 RuntimeError
```

### 5.2 桩与夹具（conftest.py）

```python
# conftest.py
import pytest
import random
from game_core import GameState, Difficulty

@pytest.fixture
def rng():
    return random.Random(42)  # 固定 seed，UT 可复现

@pytest.fixture
def default_state(rng):
    return GameState(width=20, height=15, difficulty=Difficulty.MEDIUM, rng=rng)

@pytest.fixture
def small_state(rng):
    """5×5 网格，便于穷举"""
    return GameState(width=5, height=5, difficulty=Difficulty.MEDIUM, rng=rng)
```

### 5.3 断言规范

- **不变量优先**：每个 UT 至少断言一条 INV（1~6）
- **纯函数性质**：先取 `before = state.snapshot()`，再 `after = state.step()`，断言 `before == state.snapshot()`（state 自身未变）
- **覆盖分支**：每个 step 内部分支（撞墙/撞自身/吃食/普通移动/终态）独立测试
- **参数化**：`@pytest.mark.parametrize` 用于方向枚举、难度枚举
- **UT 命名**：`test_{动作}_{场景}_{期望}`，如 `test_step_hits_wall_sets_over`

### 5.4 必须覆盖的 UT 用例清单（FO 必写）

| # | 场景 | 断言 |
|---|------|------|
| 1 | 初始构造 | 蛇长 3、初始方向 RIGHT、score=0、status=RUN、food 不在蛇身 |
| 2 | 普通前进 | step 后蛇头按方向移动、蛇尾移除、score 不变 |
| 3 | 吃食 | step 后蛇长+1、score+1、新食物不在蛇身（用固定 seed 验证坐标） |
| 4 | 撞墙 | 头出界 → status=OVER，分数/蛇身不变 |
| 5 | 撞自身 | 头撞非尾身段 → status=OVER |
| 6 | 撞尾 | 头撞尾段 → status=OVER（v1 行为一致） |
| 7 | OVER 保护 | status=OVER 后 step 抛 InvalidStateError |
| 8 | OVER 保护 | status=OVER 后 set_direction 抛 InvalidStateError |
| 9 | 反向禁止 | set_direction(opposite) 后 step，蛇仍按原方向走 |
| 10 | 幂等 | set_direction(同方向) 返回值与 self.snapshot 相等 |
| 11 | pending 合并 | set_direction(UP) → set_direction(LEFT) → step，按 LEFT 走 |
| 12 | 固定 seed | 同 seed 两次构造 → 初始食物坐标一致 |
| 13 | snapshot 不可变 | snapshot 字段不能赋值（frozen dataclass） |
| 14 | 非法网格 | `GameState(2,2,...)` 抛 ValueError |
| 15 | 难度参数 | 三档 base_tick_ms = 250/160/100 |
| 16 | 食物全屏 | 构造一个 snake 填满 5×5 → spawn_food 抛 RuntimeError（直接调内部函数测） |
| 17 | 帧内 set_direction 不立即变 direction | set_direction(UP) 后 state.direction 仍为 RIGHT（pending 隔离） |
| 18 | step 后清 pending | step 一次后 pending_direction=None，新 set_direction 不被旧 pending 污染 |
| 19 | 端到端 100 步 | 固定 seed 跑 100 步，验证 score 变化曲线与蛇身长度一致（回归基线） |
| 20 | snapshot.tick_ms | 迭代 1：== difficulty.base_tick_ms |

### 5.5 覆盖率目标

- **行覆盖 ≥ 95%**（`state.py` 必须 100%）
- **分支覆盖 ≥ 90%**（step 五大分支、set_direction 三大分支）
- **不变量测试**：每条 INV 至少 1 个用例引用

### 5.6 UT 运行命令

```bash
# 单跑本模块
python3 -m unittest discover -s tests/test_game_core -v

# 或 pytest
pytest tests/test_game_core -v --cov=game_core --cov-branch --cov-fail-under=95
```

> 框架选型：unittest 即可（对齐 v1）；pytest 仅作推荐（如团队偏好）。**强制**：core UT 不依赖 pygame/PyInstaller 等任何 GUI 工具，能在 CI 干净容器里直接 `python3 -m unittest` 通过。

### 5.7 FO TDD 实施步骤（建议）

1. 写 `test_state_init.py` → 跑（红）→ 写 `state.__init__`（绿）
2. 写 `test_state_step_move.py` → 跑（红）→ 写 `step` 普通移动分支
3. 写 `test_state_step_eat.py` → 补全吃食分支
4. 写 `test_state_step_collide_*.py` → 补全撞墙/撞自身
5. 写 `test_state_set_direction_*.py` → 补全 set_direction
6. 写 `test_state_over_guard.py` → 补全状态保护
7. 写 `test_state_food_spawn.py` → 补全食物生成
8. 写 `test_state_determinism.py` → 验收 RNG 注入

> 严格 RED-GREEN-REFACTOR，UT 写完先红，实现只补到变绿，**不要超前写未测代码**。

---

## 附录 A：迭代 1 → 迭代 2 增量接口预告（仅供 FO 留扩展点，不在本次实现）

- `GameState.toggle_pause() -> GameState`：RUN↔PAUSED
- `GameState` 构造或 `set_score_callback(cb)`：`on_score(score: int)` 事件（对接 platform-storage）
- `params.speed_curve(score: int, difficulty: Difficulty) -> int` 替换 `base_tick_ms` 直读
- `Snapshot` 增加 `tick_ms` 由 `speed_curve` 算得

接口扩展原则：默认参数 + 新增方法，**不破坏迭代 1 既有签名**。
